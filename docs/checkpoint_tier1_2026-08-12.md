# TIER-1 CHECKPOINT — options-inventory campaign (2026-08-12, FINAL)

Operator decision point per the approved tier plan. Five blocks, five
verdicts, every one measured against a gate pre-declared before the run
(pre-registration commits: abf7695 precedent; this campaign feb9917).
Production untouched throughout — verification at the end of this doc.

## The five verdicts

### 1. D1 — conservation pass under activation (2026-08-11): G-D1b FAIL
CTRL 53.4/42.9/44.0 → CONS 47.6/40.0/43.1. Corrected the retirement's own
stated reason: coverage was not the only defect; "legacy always wins" is a
second, independent one.

### 2. E4 — the ft2 re-test (G-A4, owed since 2026-07-24): STANDS DOWN, re-earned
16 replays, zero detect/track hours. ft2 < control at 8 of 10 clean
windows; no camera meets re-entry (G-E4b). The campaign's most
load-bearing "dead" conclusion is now closed on evidence gathered UNDER
today's chain. Two new mechanism findings:
- **The detection basis controls pair activation** — measured in BOTH
  directions (cam3 extension lifted blind coverage past the 0.45 bar;
  cam2 ft2 sinks it 0.564→0.385). The pre-registered guard caught what
  would otherwise have been a repeat of the G-A3 confound.
- **First direct measurement of the pair's worth on the base basis at
  cam2**: +5.3/+4.0/+3.3 5/95 points.
- Named Tier-4 candidate: cam5-scoped ft2 (right-turn cells go from
  ~zero recall to ~truth: NB_right 5/4/0→34/49/36 vs Mio 21/26/35);
  fails re-entry only on study_0700's -7.7.
- ft2@cam2 detection-only signature is a TRADE: NB_left raw recall
  IMPROVES (146→325 toward Mio 411) while 640-inference far-field
  SB_thru collapses (1979→1324 vs Mio 2247) — pointer for A1.

### 3. C-1 — evidence-ranked chain arbitration (D1 iteration 2): G-C1 FAIL
46.6/40.0/41.3 vs CTRL 53.4/42.9/44.0 — worse than iteration 1's blanket
at 2 of 3, while the rejection-split condition PASSED (vs_legacy halved;
legacy_rejected 125/92/197). The mechanism did what it was told and lost.
**LEDGER: the conservation family is CLOSED on the customer bar** (both
iterations spent from zero). The cited per-approach proof (9.1→3.3-3.5%)
does not transfer to per-cell per-bin. Event-level gate agreement is not
a sufficient statistic for per-bin correctness — G-P1's lesson, now
measured inside the chain arbiter itself.

### 4. B1 — structural twin channel, isolated: G-B1i FAIL (kill gate)
Twin instrument built (synthetic double-box + queued-follower and
lane-neighbor decoys); recall 0.067-0.155 vs the 0.80 bar across four
tables/two cameras — a mid-life duplicate has no fork/join topology, so
the signature is structurally blind to the class. Follower false-flag
0.079-0.158 (the co-life IoU channel's queue-weld failure, reproduced).
**The twin class at event level is CLOSED — five channels dead** (geometry
×2, appearance ×1, co-life IoU ×1, structural ×1). Counting arms never
ran; the kill-gate-first order saved the harness build + 6 replays.

### 5. H1 — per-regime tracker hybrid, GT-free: G-H1k FAIL (kill gate)
G-H1p PASSED — h1base reproduced committed d1ctrl CELL-FOR-CELL at all
three windows (basis stable; doubles as C-1's end-to-end OFF-parity
proof). The activation guard fired at 2 of 3 windows (ocsort dumps sink
coverage to 0.397/0.388); flag-state-matched pairs run per the standing
rule. Kill gate on {EB_left, NB_left}: OC HALVES the deficit at 0700
(77→39; EB_left err 14→2), cuts 16% at 1100 (520→438), INVERTS at the
dense 1600 (384→469). Summed cut +3.6% vs the pre-declared +10% minimum
→ FAIL; block stopped with zero merge runs and no cam4/5 compute.
**LEDGER: the regime hybrid does not survive the V2 chain / per-bin
bar** — the premise is window-dependent, not uniform, and the ocsort
dumps additionally surrender the evidence pair at 2 of 3 windows under
the shipped config. The 2026-06 result recorded as non-transferring.
Third coupling instance: after extension (raises) and ft2 (sinks), the
TRACKER choice also controls pair activation.

## Tier-2 sweep (completed, interleaved): research_synthesis_queued_2026-08-12
23 sourced facts. Directly actionable: A1+B2 confirmed as the right first
build with named mechanisms; A2 viable ONLY dual-rate (naive motion mask
DELETES stopped vehicles); per-movement source selection validated in the
literature (H1's shape); NEW option class — fragment-to-movement
classification (16%→4-5% error on fragments that never cross both gates),
the direct literature answer to the EB-left queue damage.

## What the evidence now says about Tier 3 (for the operator)

1. **A1+B2 (tubelet stabilization + zero-velocity) remains the only lever
   aimed at the real wall, and E4+H1 both sharpened its target**: ft2
   proved extra raw detections do NOT convert to counts under the current
   birth/assembly chain (insufficient-track counts ~doubled at every
   camera), and H1 proved a different TRACKER doesn't fix the conversion
   either (window-dependent at best). The conversion step is exactly what
   A1 builds. The sweep supplies the mechanism menu (BoostTrack-style
   rescoring, YOLOV pre-NMS aggregation, move-stop-move stop-state) —
   and E4's cam2 finding that ft2's raw NB_left recall IMPROVES while
   being swamped says the signal A1 needs is PRESENT in the detections.
2. **Fragment-to-movement classification enters as a first-class option**
   (from the sweep + B1's closure): the twin/dedup route to the queue
   damage is measured dead; the count-the-fragment route has literature
   evidence (16%→4-5% error on fragments) and attacks the same cells.
3. **The evidence-pair family is now fully mapped at cam2**: worth
   +5.3/+4.0/+3.3 on base; the conservation extras subtract from it; the
   arbitration variants subtract more. Nothing in the family remains
   unmeasured; no further iterations are owed.
4. **The ACTIVATION-COUPLING law, measured three ways today, binds ALL
   future Tier-3 work**: extension raises blind coverage past the bar,
   ft2 sinks it, ocsort sinks it. Any mechanism that changes track
   composition changes pair activation. Every future arm records
   `result.evidence_activation` and voids flag-state-mismatched
   comparisons — this is now standing law, not a per-block contingency.
5. **Selector/adjudication constraint stands**: gate-evidence census
   signals are circular (G-P1); C-1 adds that event-level gate agreement
   is not a sufficient statistic for per-bin correctness even INSIDE a
   mechanism.

## Recommendation (mine, for the operator's decision)

Proceed to Tier 3 with A1+B2 as ONE block (the sweep's mechanism menu,
synthetic-fragmentation kill gate pre-declared before build), and add a
cheap Tier-3 pilot of the FRAGMENT-TO-MOVEMENT classifier (KDE likelihood
maps over operator channels — GT-free by construction, prototypes from
the window's own full journeys) since it attacks EB_left directly and
its kill gate is cheap. Tier-4 candidates on record: cam5-scoped ft2
(right-turn resurrection + 0700 damage split), A4 scale-matched 1280
(E4's SB_thru collapse at 640 is its motivation), A2 strictly dual-rate.

## Ledger deltas from this campaign day

- options_inventory E4 → CLOSED (re-earned under today's chain).
- options_inventory C1/C2/D1 → resolved to the conservation ledger line
  (family dead on the customer bar, two iterations from zero spent).
- options_inventory B1 → CLOSED (fifth twin channel dead; instrument
  survives as the bar for any future twin proposal).
- NEW Tier-4 candidates: cam5-scoped ft2 (+ study_0700 damage split);
  activation-coverage coupling applies to ALL future detection work.
- NEW option: fragment-to-movement classifier (sweep F14/F15/F16).

## Production verification (run 2026-08-12 at campaign end) — VERIFIED

- cam2 study_1600 production: **6835 events** over frames 1439950-1619950,
  read-only connection — exact match.
- project.db mtime: **2026-08-10 13:45** — unchanged through the entire
  campaign day.
- All measurement in _replay_scratch/<block>/ workdirs; apply=False
  hard-coded in the CLI used by every chain; no adjudicate_apply run.
- Tests: 947 green (baseline 938 + 9 C-1). All new flags default OFF;
  C-1's OFF path parity-proven twice (unit pins + G-H1p's cell-for-cell
  reproduction of committed controls).

## Costs (actuals)

  E4:   16 replays, ~75 min compute, 0 detect/track h (dumps existed)
  C-1:  code+tests (947 green) + 3 replays ~20 min
  B1:   instrument build + 4 runs ~15 min; counting arms SAVED by kill gate
  H1:   3 ocsort pass-1s (~50 min) + 8 replays (~35 min); merges SAVED by
        kill gate; cam4/5 extension never spent
  Sweep: interleaved, zero compute contention
  TOTAL: one working day, wall-clock, for all of Tier 1 + the sweep —
  against the plan's 3-4 day estimate; the difference is the kill gates
  firing early and every replay input already being cached.

## Commits (the evidence chain, in order)

  feb9917  pre-declared gates for E4, C-1, B1, H1 (before any run)
  4259550  Tier-2 sweep synthesis (23 sourced facts)
  12b9c58  Block E4 verdict + 16 score JSONs
  15bf32f  Block B1 kill-gate verdict + instrument + 4 twin JSONs
  9527f93  Block C-1 verdict + implementation + tests + 3 score JSONs
  4fa8cdf  Block H1 verdict + chains + revival-kit merge script + 8 JSONs
