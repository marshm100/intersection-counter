# Handoff — 2026-07-06 (session end)

**Read this first, then `docs/MASTER_PLAN.md`.** Branch `claude/accuracy-impl-2026-05-27`,
**all work committed AND pushed to origin** (HEAD `8e33cf1`). Working tree clean. This was a
long, productive session: the macro-plan phases are essentially complete and the FM51 audit is
largely worked through — the one big accuracy item left is the **per-approach §1b gap (BEV)**.

---

## What shipped this session (all committed + pushed)

1. `3772e89` — **Shared-exit collinear tiebreak scaffolding.** ENTRY-proximity tiebreak for the
   cam2 SB↔EB collinear swap: IMPLEMENTED, VALIDATED, **DISPROVEN** (regressed EB 33→41 — fires on
   the misleading minority of SB tracks born mid-approach). Kept OFF, scaffolding retained.
2. `e2ec7ca` — **Speed-tiebreak productized.** SAME cluster, split by pixel-SPEED signature
   (`expected_speed` per path). Net-positive but PER-CAMERA (cam2 EB 33→29, cam5 NB 9→5.3; inert
   cam1/cam3; **regresses cam4** → default OFF, per-cam `calib_speed_tiebreak` knob). Schema +
   `scripts/backfill_expected_speed.py`.
3. `b384735` — **Weekly report** (`docs/weekly_report_2026-06-27_to_07-03.md`).
4. `aac5bd2` — **§3-D articulated classifier.** View-invariant SIZE post-pass (truck length vs
   local car baseline; bbox aspect ratio fails at approach-angle cameras). FM51 **~70%** (71 vs
   Miovision 102). `services/articulated.py`, wired into the v3 finally-block.
5. `0162c82` — **Articulated tightening.** Store per-vehicle max bbox at classify time
   (schema + pipeline) → post-pass reads stored bbox = **0 undecidable** for new runs (fallback
   cache-linker made thorough for old runs). Honest FM51 = **71** (the earlier 82 was biased).
6. `8e33cf1` — **FM51 audit #4 fix.** Side-road over-count **105→72** (+75%→+20%).
   `services/through_gate.py` rejects a 'through' between a pair the bank labels a TURN. **Per-
   camera validated tool, NOT a blind default** (corridor regression: cam3 has 691 real
   left-turners on a bank-left pair — rejecting would delete real vehicles). Applied to FM51 cam2.

**Verify-live (done):** real `process_cached` run confirmed the pipeline write path + bbox
storage + stored-bbox reclassify on real tracking output; speed bank-plumbing + knob verified.
The **v3 orchestrator finally-block auto-firing reclassify is INFERRED, not run end-to-end** (a
full-YOLO run OOMs at ~1 GB free for a trivial guarded loop).

---

## ▶ IMMEDIATE RESUME: the per-approach §1b gap — BEV de-risk on cam2

This is **the last big hurdle before production-ready** (per-approach interval MAE ≤5%). Two
independent deep-research passes (papers/GitHub + X) converged (in `MASTER_PLAN §3-D` +
[[project_per_approach_attribution_2026_06_30]]):

- **Image-space is EXHAUSTED** — the SOTA hybrid (Jana et al. arXiv 2111.09171) IS already our
  `mdh` cost. Collinear pairs share the exit + are near-identical in-image; only the FOV-clipped
  entry differs.
- **BEV / inverse-perspective-mapping = THE top bet.** The swap overlaps in the IMAGE but is
  distinct in WORLD space. **NEXT ACTION = a cheap DE-RISK** (data-in-hand, no pipeline code):
  rough homography for cam2 → project stored SB-thru vs EB-right trajectories → measure whether
  they separate (vs the fragile 84% image-space birth split). Separates ⇒ build the BEV matcher +
  auto-cal. Doesn't (divergence FOV-clipped) ⇒ fall back to the anchor.
- **Entry-line/conservation anchor = the robust blind hedge** — run in parallel, low-risk either
  way. Detector fine-tune (UA-DETRAC + OpenVINO INT8) = parallel quality track.

## Loose ends (queued, lower priority)
- **Land the validated wins to the live study project (97a7849a):** flip `calib_speed_tiebreak=1`
  for cam2/cam5 + surgical re-attribute (per the `ship_sbleft_cam2` pattern). `expected_speed` is
  ALREADY backfilled on corridor cam2 (inert, knob off).
- **`--apply` articulated + through-gate to FM51's own DB** for its deliverable (articulated not
  yet applied to FM51; through-gate IS applied).
- **Full-YOLO app run** to confirm the articulated orchestrator finally-block fires live (needs
  RAM freed + a short trim).
- **Deliverable parity spot-check** — Excel/PDF vs the Miovision examples in `docs/historic data/`.

---

## DB state changes this session (all safe / additive / reversible)
- **FM51 (0acb12c0) + corridor (97a7849a)** DBs migrated → new nullable columns (bbox_length,
  bbox_center_y on vehicle_events; expected_speed, calib_speed_tiebreak).
- **corridor cam2:** `expected_speed` backfilled on 11 paths (INERT — knob is off).
- **FM51 cam2:** 33 events marked `rejected=1` (the through-gate fix; reversible).
- No live behavior changed on the study project (speed knob left off).

## Per-camera tools that are NOT blind defaults (validate before enabling elsewhere)
- **speed-tiebreak** (`calib_speed_tiebreak`) — regresses cam4; on = cam2/cam5 only.
- **through-gate** (`services/through_gate.reject_invalid_throughs`) — would delete 691 real
  cam3 vehicles; run only where validated vs GT. Deliberately un-wired from the pipeline.

## Environment / gotchas
- **8 GB box + OneDrive DB.** Free RAM sat ~1 GB much of the session; **replays + YOLO OOM**.
  LIGHT paths that WORK: `process_cached` (no YOLO), streaming pure-`sqlite3` diagnostics,
  `backfill_expected_speed` (streams). AVOID at low RAM: `replay_fullchain` (pandas + 17k blobs),
  full `process_video`/YOLO. Free apps (close Edge/WebView2) for heavy runs — the user did it fast.
- **Reading the OneDrive detection-cache parquet in place hits WinError 5** — copy to temp first.
- **Un-migrated old DBs:** `get_connection(project)` runs the (fast, additive) column migrations;
  raw-sqlite tools that read new columns must PRAGMA-check or the DB must be migrated first.
- `reclassify_articulated` / `reject_invalid_throughs` use `get_db_path` (PROJECTS_DIR-aware).
- App server: `py start_server.py` on **port 5000** (not 8000); holds `project.db` (WAL) — stop
  before DB writes; `tasklist` is authoritative for its PID ([[reference_server_process_kill]]).
- Weekly-report gotcha: **verify work-DAYS with the user** — the harness clock lagged across a
  reboot and mis-dated Friday's work ([[feedback-weekly-report-hours]]).

## Memory to load (auto-memory, persists across the restart)
- [[project_per_approach_attribution_2026_06_30]] — the per-approach thread: entry DISPROVEN,
  speed partial+productized, the 2026-07-06 BEV research synthesis + the FM51 #4 fix.
- [[project_articulated_classification_2026_07_06]] — §3-D articulated (built, ~70%, tightened,
  verify-live).
- [[project_fm51_audit_master_plan]] — the FM51 audit (all 4 items, #4 fixed) → `MASTER_PLAN.md`.
- [[project_lowlight_wall_spotwindow_2026_07_02]], [[project_interval_metric_baseline]].

## Git
Branch `claude/accuracy-impl-2026-05-27`, HEAD `8e33cf1`, **all pushed** to origin. Clean tree.
