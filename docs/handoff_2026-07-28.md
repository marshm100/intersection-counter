# Handoff — 2026-07-27 → 07-28 run (wall B → wall C, one continuous arc)

Everything committed on `claude/accuracy-impl-2026-05-27` and pushed
(fa053ab..0f7a2c6 + the sweep block that follows this doc). Working tree
clean at each milestone; 824 tests green (up from 816: +5 path_divergence,
+3 spot-check); no processes left running. project.db untouched by every
research block; the ONLY product-code change of the run is the §3-B gate
hardening (spot_check.py), shipped with tests.

## The arc, in order (each with its evidence)

1. **Wall B (cam5 bank hole) — RETIRED** (`plan_cam5_eb_bankhole_2026-07-27.md`,
   fa053ab): phase-0 decomposition (OFF-parity EXACT 7.2/16.1/17.3/5.1)
   + fate ledgers killed the framing — holes hold 45/82/65 veh/day vs
   200–650-veh walls; the one guard-surviving fitted-path promotion
   FAILED its phantom gate (43-vs-4, cancellation masquerade; path-fill
   now dead ×3). REAL walls named+sized: far-field same-anchor STUB class
   (45% of tracks; feeds NB-left +646 AND correctly carries NB-thru),
   SB-thru insuf leak −417, EB-right journey formation −207. A GT-free
   geometric promotion guard (subsegment-of-banked-path, 15–22 px tell)
   is on record.
2. **SB-insuf leak DIAGNOSED TO THE CONSTANT** (678f2df, same plan doc):
   adjacent-lane throughs rejected by
   `score_destination_by_polyline(max_avg_distance_px=20)` against the
   lane-biased SB fit — dropped tracks ride ≥20.1 px (constant to the
   decimal), journey-complete, correctly origined; NB mirror drops 0.
   Coupling measured: un-drop alone overshoots +7.5/+8.7% (fragment
   echoes of other vehicles fill the cell).
3. **LANE+ECHO mechanism cycle — run and CLOSED per its two-iteration
   budget** (`plan_cam5_lane_echo_2026-07-27.md`, 9006c18..5094be3):
   phase-0 chain census over SIX cameras (E-prize 1,625 excess
   events/day at cam5; FM51 carries 785 direction-flip false pairs/day
   → the direction gate is load-bearing, proven on the held-out site
   BEFORE any arm; activation signal = flip-vs-same-cell COMPOSITION,
   not excess rate) + divergence geometry service (ed545f2, tested).
   Arms E → EC → LEC → LEC2 under enforced 0700-only fit discipline:
   **iteration-1 PARTIAL — NB-left CLOSED and holds held-out
   (1.85→1.06/1.07), per-cell abs −44%/−37%, protected-thru truth bands
   MISSED held-out (SB 1.054 / NB 1.085)**; iteration-2 (all-events
   concurrent dedup, IoU+lockstep) FAILED AT FIT — no discriminating
   regime; 2026-07-09's conclusion re-confirmed stronger. Nothing wired;
   LEC2 bundle frozen and revival-ready; retirements ledger grew.
4. **ReID twin SPIKE — FAIL, decisive** (`spike_reid_twin_2026-07-28.md`,
   7610751): AUC seq-vs-near 0.399 (worse than chance; near-neighbor
   pairs embed MORE similar than same-vehicle-across-depths; twins score
   lowest). At 640×480, far-band crops (10–30 px) carry no identity —
   THREE mechanism families now measured dead on far-band concurrent
   twins → reclassified a **SOURCE-RESOLUTION wall**; higher-res site
   video is the capability-track prerequisite. Died at spike cost — no
   cache builds.
5. **Wall C (§3-B blind-gate validation) — DONE**
   (`plan_3b_validation_2026-07-28.md`, f1f3d96..0f7a2c6): gate-vs-GT
   confusion matrix, 6 cameras × 5 seeds, simulated-perfect operator.
   Gate HARDENED (shipped): per-approach binding rows (first catch:
   crossed N/S labels in our own test fixture), trim-scoped
   certification, extend-to-certify protocol. cam5's false-pass caught
   4/5 by approach binding; residual 1/5 (and cam4 3/5) measured
   STRUCTURAL (net cancels to ~0; approach CIs straddle at feasible
   volumes). FM51 founding case caught end-to-end (stratified 0/5
   certify; PM window fails by name 4/5). **THE CLAIM STRUCTURE is
   written into the plan doc + new_site_runbook.md**: net-total ±5% is
   spot-CERTIFIABLE; the per-approach bar is NOT blind-certifiable by
   spot counts (measured structural limit, stated) — it rides the flag
   queue + approach tripwires; certification scope = declared trims.

## Where the bar stands (unchanged production numbers)

Corridor production untouched all run: cam1 4.0 / cam2 4.8 / cam3 3.2 /
cam4 4.7 / cam5 7.2 totals (their recorded bases); FM51 3.0 held-out.
The walls' NEW shape: per-approach residuals at cam5/cam2 are
substantially far-band concurrent-twin class = source-resolution-walled;
NB-left's closure exists in the frozen LEC2 bundle, unshipped.

## In flight at handoff time

**Operator-authorized deliverable-truthfulness SWEEP** ("handoff then
sweep", 2026-07-28): the LEC2 frozen bundle over the corridor + FM51 at
a NEW, pre-declared ship bar (per-cell truthfulness + certifiability,
NOT the missed dev bands — an explicit operator bar change, on record).
Bar + results land in `plan_cam5_lane_echo_2026-07-28` sweep section.

## Owed / parked

- Higher-res source video procurement (the named physical prerequisite
  for every remaining twin-class wall) — operator/runbook question.
- cam3 trims decision (its 24-h claim honestly refuses night
  certification; declaring peak trims scopes it).
- §3-B residuals: acceptance-composition simulation; S1 sweep (demoted
  to alarm-quality). Prior blocks': fm51_native_artic_gate outp param,
  backup rotation, subprocess job runner, semi label round.
- LEC2 ship decision = the sweep in flight.

## Operational notes (new this run)

- **%TEMP% scratch was PURGED mid-session by Windows** — replay working
  DBs now live in `data/projects/<proj>/_replay_scratch/` (gitignored
  via data/); memory saved (`temp-scratch-purged-mid-session`). Detached
  work: Start-Process + Monitor, NEVER background Bash (re-learned
  twice this run — two killed jobs).
- Corridor BASE replay DBs for all cams + FM51 ftv2n sit in the repo
  scratch dirs (re-derivable ~2–3 min/window; cam3's 24-h ≈ 27 min).
- Scoring-basis discipline held throughout: every number in every doc
  names its basis; OFF-parity re-proven after every re-derive.
