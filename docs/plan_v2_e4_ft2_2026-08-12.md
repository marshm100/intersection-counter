# Block E4 — the ft2 re-test under today's chain (G-A4, owed since 2026-07-24)
# (2026-08-12)

Tier-1 block 2 of the options-inventory campaign
(`docs/options_inventory_2026-08-11.md`, category E — "concluded on a
confounded or too-blunt instrument", item E4: "the campaign's single most
load-bearing 'dead' conclusion").

## The claim this block tests

2026-07-24 (`docs/ft2_corridor_rebaseline_2026-07-24.md`,
`phase0_diagnostics:73`): "detection recall is not the bottleneck;
association is" — the fine-tuned detector AMPLIFIED each camera's defect.
That was measured BEFORE any assembly, extension, evidence-pair, or
activation machinery existed. **G-A4 was pre-declared to re-test the ft2
disposition once association improved, and was never reached.** The chain
has since changed (evidence activation default ON 2026-07-27; the shipped
pair; corrected attribution of the applied gains). This block runs G-A4.

## Basis (verified from sidecars 2026-08-12, not from names)

- ft2 arms: `yolo26s_ft2_openvino_model` @ imgsz 640. Controls:
  `yolo26s.pt` @ imgsz 960, conf 0.1. The ft2 basis moves model AND
  inference size TOGETHER — inherent to ft2 (trained at 640, runs at 640)
  and IDENTICAL to the pairing in the original 2026-07-24 comparison, so
  this re-test is apples-to-apples with the disposition it re-tests. It is
  NOT a per-variable decomposition (that is option A4, scale-matched 1280,
  Tier 4).
- Pass-1 dump recipes match base per camera (cam1 botsort+reid m0.8; cam2
  botsort nms0.85 m0.9; cam4/5 bytetrack m0.8) — only detection source
  differs. All 10 ft2 dumps complete on disk; ZERO detect or track hours.
- Anomaly, recorded: cam4 study_1600 ft2 dump ends at frame 647963 vs base
  647980 — 17 frames (1.7 s) short at the window tail. Negligible vs a 2 h
  window; noted so nobody rediscovers it as a mystery.
- Arms: cam2×3, cam4×3, cam5×3, cam1 study_0700 (the 10 windows with both
  an ft2 dump and a committed base control). cam3 excluded: no stock
  counterfactual (~19 h to build one).
- Controls (committed): `score_d1ctrl_cam2_study_*` = 53.4 / 42.9 / 44.0;
  `score_ga3ctrl_cam1_study_0700` = 60.5; `score_ga3ctrl_cam4_study_*` =
  75.4 / 69.4 / 75.8; `score_ga3ctrl_cam5_study_*` = 66.4 / 71.7 / 63.1.
  Both control families are base dumps + default flags — same basis as
  these arms (env: all mechanism vars explicitly UNSET).

## Mechanics

Chain script `runs/e4_ft2/e4_chain.ps1` (armC pattern): per window, fresh
workdir `data/projects/97a7849a/_replay_scratch/e4_ft2/cam<C>_<W>/`, env
vars explicitly removed (defaults), `py -X utf8 scripts/v2_run_pass2.py
--camera <C> --variant ft2_study_<W> --workdir <wd>`, WAL-safe copy
(sqlite3 backup API — several scratch DBs carry live -wal sidecars) to
stem `e4ft2_cam<C>_study_<W>.db`, `py -X utf8 scripts/v2_score_dev.py` on
the copy, then DELETE both DBs (disk discipline; score JSON + .stats.json
sidecar are the evidence; re-derive ≈2-8 min/window on demand). No scorer
change: the copy-to-stem convention resolves the window (d1ctrl
precedent); `ft2_` stays out of the v2_score_dev.py:172 prefix tuple.
Sequential; detached via Start-Process + log + Monitor.

## GATES — declared before any run; numbers final

- **G-E4a (the owed question):** per window, ft2 vs its control on 5/95
  per-cell per-bin. "Still amplifies the defect" = ft2 < control.
  Report per-window delta, compliant/scored pairs (cells_scored differs
  per arm — either-source cells — so pct alone is not the row), and the
  totals table for the named deficit cells.
- **G-E4b (disposition):** ft2 re-enters candidacy at a camera only if
  ≥ +1.0 on ≥ 2 of that camera's windows AND ≥ 0 on the rest of them.
  Otherwise the 2026-07-24 disposition STANDS, re-earned under today's
  chain, and E4 is closed honestly (no longer "measured before assembly
  existed").
- **Activation contingency (pre-registered — the G-A3 confound shape):**
  ft2 shifts detections → origin-evidence coverage shifts → the activation
  precondition (default ON, bar 0.45) can flip the gate+posterior pair
  between an arm and its control. Control states on record: cam2 ACTIVATED
  0.564/0.487/0.485; cam1 NOT 0.265/0.438 (0700 relevant here); cam4 NOT
  0.304/0.442/0.318; cam5 NOT 0.330/0.441/0.402. Margins at cam4/cam5
  study_1100 are −0.008/−0.009 — live flip risk. RULE: record
  `result.evidence_activation` from every arm sidecar; any arm whose
  activated state ≠ its control's makes the default-flags comparison VOID
  at that window; run supplementary arm + control with
  `EVIDENCE_ACTIVATION_ENABLED=0` (fresh workdirs, stems `e4ft2x_*` /
  `e4ctrlx_*`) and evaluate G-E4a/b on the flag-off pair, reporting the
  decomposition both ways (the v2_confound_table.py method). No undeclared
  fourth variable.

## Traps live in this block

Variant names encode no basis (sidecars read and recorded above); own
workdir per arm (sidecar_reusable ignores env); distinct `e4ft2` stems
(scorer overwrites by stem); WAL-safe copies only; sequential runs
(~700 MB free RAM); background Bash is reaped — detached chain + Monitor.

---

## RESULTS (2026-08-12; 10/10 windows ran clean, ~30 min wall)

  window            ctrl            ft2             delta   act ctrl->arm
  cam2 study_0700  53.4 55/103    39.3 44/112    -14.1   ON  -> OFF (0.564->0.385)  VOID*
  cam2 study_1100  42.9 45/105    36.4 40/110     -6.5   ON  -> OFF (0.487->0.320)  VOID*
  cam2 study_1600  44.0 48/109    27.0 30/111    -17.0   ON  -> OFF (0.485->0.329)  VOID*
  cam4 study_0700  75.4 43/57     57.9 33/57     -17.5   OFF -> OFF  valid
  cam4 study_1100  69.4 34/49     53.7 29/54     -15.7   OFF -> OFF  valid
  cam4 study_1600  75.8 50/66     66.2 45/68      -9.6   OFF -> OFF  valid
  cam5 study_0700  66.4 71/107    58.7 64/109     -7.7   OFF -> OFF  valid
  cam5 study_1100  71.7 76/106    78.5 84/107     +6.8   OFF -> OFF  valid
  cam5 study_1600  63.1 65/103    66.7 72/108     +3.6   OFF -> OFF  valid
  cam1 study_0700  60.5 49/81     52.8 56/106     -7.7   OFF -> OFF  valid

*cam2: the PRE-REGISTERED ACTIVATION CONTINGENCY FIRED at all three
windows — a NEW MECHANISM FINDING. ft2 does not merely add noise tracks
(13907 vs 8320 at 0700, ~half marked insufficient): it DROPS
origin-evidence coverage below the 0.45 activation bar, standing the
shipped gate+posterior pair down. The added tracks are overwhelmingly
BLIND. The detection basis controls pair activation — the same coupling
that made cam3's extension "win" (+11.5 via pair admission), operating in
reverse. The default-flags cam2 comparison therefore moves TWO variables
and is VOID as a mechanism measurement; supplementary flag-off pairs
(e4ctrlx/e4ft2x, EVIDENCE_ACTIVATION_ENABLED=0 both sides, chain
runs/e4_ft2/e4x_chain.ps1) decide cam2's G-E4a. NOTE the deployment
reading is different and reported separately below: production runs
activation ON, so "ft2 at cam2 as shipped" = the VOID row's -14.1/-6.5/
-17.0 IS what deployment would see — mechanism attribution is what needs
the clean pair, disposition already looks terminal.

G-E4a (valid windows): ft2 < control at 5 of 7 — amplification RE-EARNED
at cam1 (with cells_scored exploding 81->106, phantom-cell growth from
2.5x track count) and cam4 (all three; NB_left deficit 1/1/2 vs Mio
20/17/23 UNMOVED — ft2's extra detections never become the missing left
turns). REVERSED at cam5 study_1100/1600.

G-E4b: ALL FOUR CAMERAS STAND DOWN under the pre-declared gate.
cam5 fails only on study_0700's -7.7 ("(>=0 on the rest" violated).

NAMED FOR THE CHECKPOINT (not actionable inside this block's budget):
cam5's right-turn resurrection is real and large — NB_right 5/4/0 ->
34/49/36 vs Mio 21/26/35; EB_right 124/116/147 -> 189/197/247 vs Mio
188/162/244 (near-exact at 0700/1600). Cells at ~zero recall for the
whole campaign reach truth on the ft2 basis. A cam5-scoped ft2 candidate
with a study_0700 damage-split diagnosis is a legitimate Tier-4 entry.

## Supplementary flag-off pairs (cam2, EVIDENCE_ACTIVATION_ENABLED=0 both
## sides — ONE variable: detection basis)

  window            ctrlx           ft2x           delta   decomposition (default = detection + pair)
  cam2 study_0700  48.1 50/104    39.3 44/112    -8.8    -14.1 = -8.8 + (-5.3)
  cam2 study_1100  38.9 42/108    36.4 40/110    -2.5     -6.5 = -2.5 + (-4.0)
  cam2 study_1600  40.7 44/108    27.0 30/111   -13.7    -17.0 = -13.7 + (-3.3)

The decomposition closes with no interaction residual. Two mechanism
facts fall out, both new:

1. **First direct measurement of the pair's worth on the BASE basis at
   cam2**: +5.3 / +4.0 / +3.3 5/95 points (48.1→53.4, 38.9→42.9,
   40.7→44.0). Context for C-1: the conservation pass (D1) was giving
   back -5.8/-2.9/-0.9 of exactly this.
2. **ft2's detection-only signature at cam2 is a TRADE, not uniform
   noise**: raw left-turn recall IMPROVES (NB_left 146→325, 105→159,
   164→260 toward Mio 411/450/420) while SB_thru COLLAPSES (1082→678,
   922→652, 1979→1324 vs Mio 1384/1064/2247) and NB_thru inflates. The
   640-inference far field (SB approach) is what dies — consistent with
   the A4 note that 640-vs-960 inference trades far-field, fine-tune or
   not. The left-turn gains ft2 finds are real and are SWAMPED — a
   pointer for A1 (stabilization could keep them without buying the
   collapse) and for the fragment-classification option from the
   2026-08-12 sweep.

## VERDICT — G-E4a CONFIRMED (re-earned), G-E4b: ALL CAMERAS STAND DOWN

- G-E4a: ft2 < control at 8 of 10 windows on clean frames (cam2 judged
  on the flag-off pairs; cam1/4/5 valid as run). The 2026-07-24
  "amplifies each camera's defect" disposition is RE-EARNED under
  today's chain — no longer "measured before assembly existed". The
  options-inventory E4 entry is CLOSED.
- G-E4b: no camera meets re-entry. cam5 comes closest (+6.8/+3.6 at
  1100/1600) and fails on study_0700's -7.7.
- NAMED, not actioned (Tier-4 candidates for the checkpoint):
  (a) cam5-scoped ft2 with a study_0700 damage-split diagnosis — its
  right-turn cells go from ~zero recall to ~truth on ft2;
  (b) the activation-coverage coupling: any detection-basis change
  moves pair activation (both directions now measured: cam3 extension
  UP past the bar, cam2 ft2 DOWN through it). Any future detection
  work MUST re-check activation state per the standing guard.
- Cost: 16 replays total (10 primary + 6 supplementary), ~75 min
  compute, zero detect/track hours. All scores committed
  (`score_e4ft2_*`, `score_e4ctrlx_*`, `score_e4ft2x_*`); working DBs
  deleted per disk discipline; stats sidecars retained in
  `_replay_scratch/e4_ft2/`.
