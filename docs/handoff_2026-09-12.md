# Handoff prompt — intersection counter, 2026-09-12

> DECISION TAKEN 2026-09-12: SHIP. The fracture rule is the default (FRACTURE_DEDUP on,
> commit bb78362); the fleet was reprocessed and the default is 77.97. Record: docs/
> plan_basis_2026-09-11.md "SHIPPED — THE FRACTURE RULE". The open-decision text below is history.

You are the agent on C:\dev\intersection-counter (Python 3.12 venv at .venv; app on 127.0.0.1:5000). Marshall dictates by voice: one question per message, carry the full context with every ask, plain prose, never a menu. Read his records before stating anything (docs/, git log); recalled memory is background, not authority. Review clips are full frame, never cropped.

## Prime directive
Cams 1-5 are test footage. The deliverable is ONE default configuration that reaches Miovision on a blank, untested intersection. Never ship per window. A change ships only as a default, judged on all 12 windows by scripts/fleet_flags.py against the G-DEF-1 letter (fleet mean rises; no camera -1.0; no window -3.0), with FM51 (project 0acb12c0, scripts/fm51_arm.py) as the blank-site witness.

## Where things stand
- The default (CLAUDE.md "The default"): fleet 76.97 over 12 windows. Standings in scripts/fleet_flags.py `_ALL`.
- THE FRACTURE RULE is built and NOT shipped: config.FRACTURE_DEDUP (default OFF), FRACTURE_GAP_S 1.0, FRACTURE_DIST_BOXES 1.0; backend/services/turn_merge.py fracture_pairs / fracture_track_dedup; hooked in two_pass.run_pass2 after the twin dedup; backend/tests/test_fracture_dedup.py (10 tests; suite 1226 green). A track that dies and another born within 1 s and 1 box length of the death point, same heading, earlier half lacking its exit, is one vehicle; the shorter event is rejected (write-then-reject).
- Marshall ruled 10/10 merged pairs "same vehicle" on reels (cam5 clips 1-2 of the fragment reel, all five cam4 pairs). The rule is correct by ruling.
- Arm d17 (rule on today's basis, 12 windows): fleet 76.97 -> 77.97 (+0.99). cam1 +1.1/0, cam2 0/+1.0/-2.8, cam3 +0.9, cam4 +2.2/+4.2/-5.2, cam5 +5.7/+1.9/+2.9. FM51 b7: +2.0 / 0. The one window past the letter is cam4 1600 (-5.2): the rule removes ~366 duplicate NB throughs that were cancelling an equal far-field detection deficit (the missing vehicles are not in the dump: scripts/census_cam4_nb_deficit.py). Ledger: docs/plan_basis_2026-09-11.md (d15, d16, d17 verdicts, all rulings).
- Arm d16 (rule + the large detector everywhere): fleet 77.44; cam3 -5.0 is the large basis, not the rule (G-DEF-8).
- Dead ends, do not re-run: pixel track-length floor (d15), time floor (census: durations identical across cameras), born-inside software rule (G-DEF-7), single-basis defaults (G-DEF-5/6), theft class (instrument floor).

## The open decision (his, not yours)
He closed the session saying "I have no idea" to: ship the fracture rule as the default now, accepting cam4 1600 at 70.2 because its old 75.4 was two errors cancelling, or hold it until the letter is met. Put that question to him first, with the numbers above, one question, and act on his answer:
- SHIP: follow the ship flow in scripts/ship_gsm1.py / reprocess_default.py (named pre-ship backup, force_once disposition, run_pass2 apply=True through the apply gate, isolation hash, health sidecars, re-score production == arm, update `_ALL` standings, CLAUDE.md "The default", memory). Flip FRACTURE_DEDUP default to "1" in backend/config.py; pin tests that assume it off.
- HOLD: leave the default at 76.97; the next candidate is whatever closes cam4 1600 without a pixel constant, declared before scoring, one arm at a time.

## Useful scripts
scripts/fleet_flags.py (FLEET_STEM, FLEET_ARMS, FLEET_WORKERS; env flags select the arm), scripts/v2_score_dev.py, scripts/fm51_arm.py, scripts/viz_fragment_pair_reel.py (pair reels, WebM full frame), scripts/diag_fracture_pairs.py (what the rule pairs on a window), the census_* scripts from 2026-09-11/12. Arm DBs live in data/projects/97a7849a/_replay_scratch/fleet_20260908/; scores in runs/v2_week1/score_<stem>_<cam>_<variant>.json.
