# Handoff — 2026-07-28 → 07-29 block (the 5/95 standard adopted; Stage 1 done)

Everything committed on `claude/accuracy-impl-2026-05-27` and pushed
(bd2d3fc..3798174; prior run in `handoff_2026-07-28.md`). Tree clean,
846+ tests green, no processes running. project.db untouched.

## What happened, in order

1. **THE STANDARD CHANGED (operator directive):** the Miovision 5/95
   rule — per movement cell per 15-min bin: ref ≤100 → ±5 vehicles;
   >100 → ≥95% — is THE bar (their conditions: qualifying footage,
   night excluded). Interval-MAE demoted to a diagnostic (§1b
   supersession note). Second directive: minimize human reliance;
   CHILD-TEST every human moment. Both saved to memory
   (`standard-595-and-child-test`).
2. **Baselines measured before planning** (scripts/rule595_compliance,
   runs/3b_validation/rule595*.json): raw cell-bin compliance cam1
   65.3 / cam2 46.7 / cam3 77.3-daylight / cam4 76.4 / cam5 70.9 /
   FM51 71.0% (current chain) — vs an effective ~100% guarantee. THEN
   the decisive one: **queue recall vs the 5/95 failures = 88.5%
   overall, 94.3% of BIG (≥20 veh)** — pipeline + worked queue is the
   credible route, matching how Miovision's own guarantee works
   (pipeline + human fix layer).
3. **The plan of record:** `plan_595_standard_2026-07-28.md` — claim =
   post-review 100% cell-bin compliance on qualifying footage at ~10×
   less human labor; six-stage execution checklist (also mirrored in
   MASTER_PLAN §1c); acceptance test pre-declared (blind qualifying-
   footage study, GT withheld until after export).
4. **STAGE 1 COMPLETE (2026-07-29):**
   - 1.3 bin-edge audit: REAL variance (shift-sweep gains ≤2.6 at
     inconsistent optima; per-site shift fitting refused as §0
     overfit).
   - 1.2 the acceptance gate binds by the 5/95 TOLERANCE + Katz-CI
     guard (±5/15-min-equivalent small, 5% large; the old ±5%-band +
     30-manual floor superseded). **§3-B matrix re-run: ZERO false-pass;
     cam5 caught 5/5 seeds (the old band managed 4/5)** — the customer
     standard's own shape closed the last structural false-pass.
   - 1.1 the rule is a service (backend/services/rule595.py, boundary
     tests; dev scorer refactored onto it, baseline byte-identical) +
     movement×class cells measured (c1 68.1 / c2 62.4 / c4 72.8 /
     c5 79.2%). Report surfaces scoped: blind = the gate's 5/95 spot
     rows (live); dev = the wall chart; export panel = Stage-4 3e.

## NEXT — Stage 2, the labor levers (the open-card 123–1,740/camera problem)

2.1 Queue PRECISION measurement (invert the recall join: noise rate per
    flag subtype on compliant cells). GATE: precision table.
2.2 Auto-resolution rules per subtype (zero-volume holes post-census,
    exact duplicates, cells already inside the ±5 grace). HARD
    PRE-DECLARED GATE: open cards drop to TENS per camera while
    BIG-failure recall stays ≥94.3% — recall may NOT drop.
2.3 Cards re-keyed to the 5/95 frame: one card = one failing cell-bin.
Then Stage 3 (census-as-star-rating at ingest) and Stage 4 (child-test
UX). Stage-5/6 [OP] moments below.

## The operator's court (unchanged, one URGENT)

- **6.2 QUALIFYING FOOTAGE procurement — the long-lead item; start
  now** (the acceptance test and every resolution-gated wall wait on
  it).
- 5.2 census-gated LEC2 activation authorization (when wanted).
- Studio dry-run (F2/F3 still await first human use).
- cam3 trims declaration.

## Standing discipline (unchanged)

Plan doc before mechanism; measured ablations; pre-declared gates;
negatives are deliverables; name the scoring basis (5/95 cell-bin vs
MAE-diagnostic vs replay bases); FM51 held-out never tuned on; detached
jobs via Start-Process + Monitor; check-and-kill port-5000 owners
before starting the server (the shadow-server lesson); replay scratch
in data/projects/<proj>/_replay_scratch, never %TEMP%.
