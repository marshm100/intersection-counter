# PPT-3 — prototype ADMISSION + saturation cap on clean data
# (2026-08-17; operator-approved plan; gates final before any run)

The iteration that follows the corridor round's ledger
(plan_ppt_corridor_2026-08-17). Grounded in the failure-anatomy
measurement (read-only agent sweep, 2026-08-17), whose load-bearing
numbers are recorded here so the design's evidence survives:

## The measured anatomy (design evidence)

- **The originally-named headroom cap is INFEASIBLE on the design
  data.** Family net_gain <= alpha*h + beta over the known good/bad
  moves has an EMPTY feasible region — two independent proofs:
  (1) at identical h=0, cam1-0700 NB_thru is a GOOD +70 (needs
  beta >= 70) while cam5-0700 WB_left is a BAD +67 (needs beta < 67);
  (2) the binding pair: cam2-1100 NB_left GOOD (+87 at h=101.6) vs
  cam5-0700 NB_right BAD (+85 at h=130.3) — the bad move has MORE
  headroom. Root cause: expected-share volume is meaningful only where
  the full census is representative; at cam5 the (37,38) cell holds
  43.6-56.0% of ALL fulls on 0.95-2.5% of real volume (its GATE
  mis-reads throughs — see the cam5 follow-up), inverting every other
  cell's expectation.
- Signals that separate, blind: RATIO = census_share/live_share (cam2
  goods max 1.43; corrupt prototypes 4-31+); intersection_paths
  existence (cam5's pathless (39,36)/(36,39)/(38,36) receive 30-55% of
  its moves; channels table is EMPTY at cam1/cam4 — unusable);
  label-consistency (cam4 (34,33): derive_movement 'through' via the
  n==2 T-branch vs stored-modal 'left' 67/74 vs path 'left' — one cell
  takes 49% of cam4's moves into a Mio-zero bin); two-sidedness
  (prototype-less cells with live mass get force-drained: cam1 24→22
  holds 161-287 events on 0-1 fulls — the EB_right −83/−95 drain);
  contradiction-rate >= 10% of the entry/exit population marks a
  structurally mis-gated camera (cam5: 21-31%, 94% exit-side, on
  exactly the two mouth pairs 160/162 px apart; others 0.5-7.7%).

## Phase A — admission (candidate-side only)

(a) RATIO floor R_MAX = 2.0 — declared rule: tightest round value
    keeping every known-good destination cell with >= 1.4x margin
    (max good 1.43). live_share from the control DB's window cells.
(b) (o,d) must have an intersection_paths row.
(c) derive_movement(o,d) must equal the cell's modal stored movement
    among the control's live events (>= 5 to evaluate; else (b) rules).
(d) TWO-SIDED: events whose current cell lacks an admitted prototype
    are never re-decided (census 'unsupported_current').
(e) CAMERA gate: contradiction_rate >= 0.10 stands the camera down
    (cam5 today, 0.21-0.31).

**G-PPT3-p (parity/reproduction, before any diagnostic):**
  - --legacy (admission off, old two-arg freeze semantics) reproduces
    the APPLIED cam2-1600 candidate census EXACTLY (reference: the
    committed ppt_c_cam2_study_1600.json — 143 reattributed and the
    identical census dict) against the retained pre-apply backup.
  - Admission spot-asserts: cam4 (34,33) rejected by (c); cam5
    pathless trio rejected by (b); cam1 (24,23) rejected by (a); ZERO
    cam2 prototype cells rejected at study_1600 on the same basis.

## Phase B — the cap, re-fit on Phase-A diagnostics

**G-PPT3-f (kill gate):** on the NEW good/bad move table from Phase-A
diagnostics (good = cell |err| reduced; bad = healthy-cell |err| grown
> 5), one rule from {net_gain(cell) <= alpha*max(exp_vol − live, 0)
[+ beta]; per-camera applicability permitted, like freeze sets} must
pass ALL goods and block ALL bads with >= 1.4x margin, constants by
the tightest-round rule. INFEASIBLE → the cap is DEAD on the record;
Phase A ships alone; cam2-0700 stays parked (its +85 NB_left is the
one failure admission cannot reach; at cam2 alone the h=0 signal
separates it from every cam2 good — per-camera applicability is the
expected resolution).

## Phase C — the standing corridor procedure

Freeze rule re-applied on the new diagnostics; candidates on live
(window-filtered); G-PPT3-a per window: zero-mass; frozen cells
byte-equal; 5/95 >= live + 1.0; no HEALTHY cell |err| grows > 5
(non-compliant-only misses batch to ONE operator ruling); calibration
floors >= 0.90/0.60. Then AG2 candidacy (gate untouched) and applies
via scripts/v2_apply_reattr.py. Targets in order: cam2 study_0700
(bar 56.8), cam1 study_0700/1600 (bars 61.5/55.5), cam4 x3 (bars
76.4/70.4/76.8). cam5 EXCLUDED by (e).

## Named follow-ups OUTSIDE this block (recorded)

- cam5 mouth-geometry repair (operator session): mouths 38↔39 160 px
  and 36↔37 162 px sit on the dominant throughs' exit legs; 609 fulls
  gate (37,38) while their stored events say 37→39. Redraw success
  criterion: contradiction rate < 0.10 AND the (37,38) census share
  collapsing toward its ~1% volume share. Then cam5 re-enters Phase C.
- cam4 derive_movement n==2 T-junction defect: live production TODAY
  bins 47-74 events/window into Mio-zero EB_thru (a LABEL error, not a
  count error). Backend fix = its own gated block with label-parity
  tests across all five cameras.

## Deliverables

scripts/v2_f2m_pilot.py (admission param), scripts/v2_ppt_reattr.py
(wiring + census + --legacy), this doc's verdict sections, the
feasibility table, per-window scores/candidacy/apply records.
Committed either way.
