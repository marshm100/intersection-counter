# Plan — V2 Block 2: residuals + G-A3 corridor blind transfer (2026-08-06)

Successor to plan_v2_week1_verdict.md (block-1 closing state: composed
stack beats control on all three cam2 windows; commit 717327e). Five
items, each with mechanism, instrument, and pre-declared gate. Same
discipline: scratch-only until gates pass, constants window-derived or
ledgered, Mio = dev scorer only, sibling-cell controls mandatory on any
selector change.

## Item 1 — SB-thru merge-interaction diagnosis (the open thread)

SYMPTOM: extension shifts the merge stage (merged_away 237->342 dev) and
SB-thru swings (-121 dev / +78 midday / -139 PM on the stack).
HYPOTHESIS: corpus bank + merge expecteds are rebuilt from the EXTENDED
dump -> per-cell expecteds shift -> the volume-gated turn merge eats
SB-thru events as turn-fragment collateral (and/or conserve_pass chains
changed by extension rows).
INSTRUMENT (pure diagnosis, no mechanism): per-window diff of merge
decisions between base and v2c runs — the stats' borderline records +
per-cell merged_away attribution; one script, existing artifacts.
FIX CANDIDATE (only after diagnosis confirms): merge expecteds from the
BASE-dump census — the same base-census principle the demotion selector
already adopted (one-line unification in run_pass2).
GATE: stack SB-thru |err| <= standalone-extension's on all 3 windows;
no merge cell regresses beyond +-2/bin noise.

## Item 2 — EB dose-vs-recall residual

SYMPTOM: stack dev EB-right 320 vs demotion-standalone 222 (Mio 167) —
extension COMPLETES additional false-origin journeys that arrive
GATE-EVIDENCED (they genuinely cross the W gate), outside the
direct-claim demotion class.
DIAGNOSIS FIRST: decompose stack EB-right by posterior_source x gate
evidence (existing SQL + gate-cross probe on the v2c dump). Also test
the standing suspicion: BACKWARD extensions manufacturing false entries.
FIX CANDIDATES (measure in this order): (a) heading-lock on backward
extension (extension velocity must stay within the track's entry-heading
cone — no turns during extension; kills false gate-crossings at source);
(b) IF (a) insufficient: extend the dose to gate-evidenced events in
flooded cells (riskier — evidence class demotion needs its own
counterfactual first, sibling-cell controls mandatory).
GATE: stack dev EB-right <= 250 with NB-left >= 405 held; held-outs
re-run blind, no 5/95 regression.

## Item 3 — Queue-order stitching for the 60 s class

SYMPTOM: instrument recall ~0.16 at 60 s gaps (red-light stops) — the
ZV hypothesis alone cannot pick among co-located queue neighbors.
MECHANISM: FIFO structure. Within a STOP ZONE (endpoint-density
clusters, machinery in v2_assemble.explained_mask), vehicles exit in
arrival order per lane: match zone-ENTERING ends to zone-LEAVING starts
by ORDER (sequence alignment over arrival/departure times + lane
projection), not pairwise cones. Implement as a dedicated ZV-linker
stage feeding the same re-ID dump path.
INSTRUMENT FIRST (the block's entry test): synthetic 60 s cuts at
min-speed points (existing v2_frag_instrument class).
GATE: 60 s recall >= 0.5 at cut_precision >= 0.9 on cam2 dev + cam1 PM
transfer BEFORE any counting integration; then the counting A/B.

## Item 4 — Time-local allocation sampling

SYMPTOM: demotion's sampled allocation uses WINDOW-GLOBAL corpus
proportions; bins can only sharpen if the mix is the event's own
15-minute segment (traffic mixes shift within a window).
MECHANISM: per-segment supports from the BASE dump's strict-census fulls
(cell counts per 15-min segment, add-one smoothed, falling back to
window proportions when a segment is thin — scale-1 per segment by
construction). The sampler draws from the event's segment distribution.
INSTRUMENT: per-bin 5/95 (the metric IS the instrument here).
GATE: 5/95 >= current stack on ALL three windows; cell totals within
noise of the current stack (this change must be bins-only).

## Item 5 — G-A3: corridor-wide blind transfer (the block's exit gate)

Freeze everything (post items 1-4; constants ledger snapshot in this
doc's verdict section). Run the FULL composed stack (extension +
demotion + contrast guard + base census + twins) on the OTHER cameras'
existing dump windows in scratch, control-vs-treatment each:
- cam1 both windows (expect: demotion STANDS DOWN via contrast guard —
  verify; extension may help its fragment floods — measure);
- cam3 (its window = the existing full-day dump; expect near-clean
  confusion — verify);
- cam4, cam5 study windows (bank/coverage differ — the honest test);
- FM51 (the held-out site, cam2 of project 0acb12c0) — blind
  generalization, no corridor knowledge.
GATE G-A3 (pre-declared, the box-clip killer applied on purpose): ZERO
per-camera edits; no camera's 5/95 regresses vs its scratch control;
every stand-down is explained by its recorded selector values (the
blind-behavior audit). PASS -> the bundle + records go to the operator
for the promotion/apply-gate decision (Phase-1 product work is the
prerequisite for any default-ON). FAIL at any camera -> that camera's
record names the next mechanism, per the ledger discipline.

## Sequencing & effort

Day 1: Items 1+2 diagnoses (scripts over existing artifacts) -> fix
candidates chosen by evidence. Day 2: fixes + three-window re-measure
(both gates). Day 3: Item 4 (small, bins-only gate). Days 3-5: Item 3
(instrument-gated; new mechanism). Day 5-6: G-A3 sweep (cam3's full-day
replay is the long pole — overnight slot) + block verdict doc.
Commit per item verdict (block-1 precedent, 717327e).


## BLOCK-2 VERDICT (2026-08-06, sweep complete)

Items: 1 SHIPPED (merge rescue, 50400fd), 2 PARKED (heading-lock,
361848a), 3 PARKED (FIFO, 437d83c), 4 LEDGERED (time-local, d1e717c).

**G-A3: FAIL for blanket application; the sweep's real product = the
applicability law.** Control vs treatment (frozen bundle, blind):
cam1 -15.7/-10.9, cam4 -15.1/-5.0/-23.7, cam5 -11.2/-8.7/-11.5 —
vs cam2 +2.4/+6.7/+3.5 and **cam3 daylight +8.5 (64.1->72.6 on 479
bins — the campaign's largest single win, over the SHIPPED production
table, zero cam3 tuning, blind)**.

THE LAW (11 windows of evidence): the bundle wins where journeys are
MISSING (occlusion truncation, attribution-starved bases) and loses
where completion MANUFACTURES journeys (oblique far-field, mid-block
births, formation walls); every losing window carries the +25-37%
event-flood signature the blind conservation guards already flag;
winners do not. CONSEQUENCE: per-window candidate-vs-incumbent
adjudication under the blind guards — THE PHASE-1 APPLY GATE — is the
required product architecture; it picks correctly at all 11 windows.
No new selector is needed or justified.

NEXT BLOCK: build the apply gate (Phase-1 of plan_miovision_replacement,
now with 11 windows of adjudication ground truth to validate against),
then the bundle rides it: cam2+cam3 apply, cam1/4/5 stand down, and
every future mechanism inherits the same discipline. FM51 held-out
adjudication joins that validation set.
