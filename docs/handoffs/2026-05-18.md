# Intersection-counter session handoff — 2026-05-18 → 2026-05-19

## Where we are
Worked through 6 bugs (#11-#16) this session. Five are fully fixed and committed.
Bug #5 (turn classifier) is **fixed and validated on real Sunnyvale TX data**:
26 mislabeled u_turns → 0; turn fraction 6% → 57%. The user reported "still
something not working properly" mid-validation — that was because uvicorn's
StatReload didn't fully cycle while the pipeline thread was alive. Hard-
restarting the server resolved it. Worth remembering: **after editing
backend Python that affects the pipeline, hard-restart the server (TaskStop
+ relaunch), not just rely on uvicorn reload.**

## The exact next decision (blocked on user input)
We were in the middle of discussing how to address: **fast mode misses
high-speed vehicles that flash through the camera FOV in <100 ms.**

The user asked: "what about if we just turn up the vehicle detection and
tracking to max sensitivity?" I pushed back — explained max sensitivity
helps weak detections, not temporal undersampling, and proposed three options:

1. **Add a "Balanced" mode** — yolo26s @ 960, detection_skip=1, lower
   activation threshold. ~5× faster than Accurate, catches fast vehicles.
2. **Crank Fast mode's sensitivity** — conf 0.15→0.08, activation
   threshold 0.25→0.15. Catches more but more false positives.
3. **Both** — three real modes in the dropdown.

I asked the user which path; they said they wanted to clarify, then asked
for this handoff. So **pick up by re-asking the mode question** and going
from there. The implementation per option is ~15-50 lines.

## What's done in this session

| Bug | Status | Files |
|---|---|---|
| #11 Home "needs video" on v3 projects | ✅ | backend/routers/projects.py |
| #12 Save-labels has no built-state feedback | ✅ | backend/routers/videos.py, frontend/js/setup.js |
| #13 Configured intersection shows "Idle / Configure" | ✅ | backend/routers/intersections.py (_is_intersection_configured), frontend/js/setup.js (chip 'idle' case split) |
| #14 No liveness indicator / ETA on chip | ✅ | backend/routers/intersections.py (_on_frame stashes frame_progress), frontend/js/setup.js (pulse dot + ETA) |
| #5 Turn classification biased toward through | ✅ FIXED, tuning open | backend/services/trajectory_classifier.py (score_destination_leg + rank-based derive_movement), pipeline.py wiring, v3_aggregator.py od_matrix |
| #6 Add fast/accurate processing-mode toggle | ✅ | backend/config.py PROCESSING_MODES, detector.py constructor args, pipeline.py detection_skip param, frontend selector |

## Bug #5 follow-ups (parked, decide whether to push)
- **Turn fraction landed at 57%** on Sunnyvale — high for typical arterials
  (usually 25-40%). Suggests destination scoring may be slightly biased
  toward turn legs vs through. heading_weight=0.7 / position_weight=0.3
  in score_destination_leg are tunable.
- **Frontend OD-matrix view** — aggregator returns it but nothing renders
  it yet. Would go alongside the existing TMC table on the summary page.
- **Excel export OD sheet** — same data, second sheet in the .xlsx.
- **Clustering tier** — the user's "trajectories obvious to the human eye"
  architecture. Per-intersection HDBSCAN over (endpoint angle from center,
  resampled trajectory shape). Use cluster membership to refine destination
  assignment. Bigger lift; geometric scoring is the cold-start fallback.

## Important quirks / state to know
- **User hardware:** Intel i5-1135G7, 8 GB RAM, Iris Xe (no CUDA).
  Detection is CPU-bound. Fast mode (yolo26s@640, detection_skip=3) gives
  ~10-15× over Accurate.
- **Sunnyvale TX project:** project_id `97a7849a`, intersection 1
  (NBeltLineRd-NorthwestDr), camera 1 (label "405051"). Legs at refs
  259/61/290/123 — skewed (not orthogonal). This is what broke the
  initial delta-bucket derive_movement; rank-based fix handles it.
- **Test suite:** 145/145 across touched modules. New classifier tests:
  TestDestinationScoring, TestDeriveMovement, TestDeriveMovementSkewedGeometry,
  TestDeriveMovementTJunction, TestEndToEndDestinationClassification.
- **CLAUDE.md hard constraints to respect:** vanilla HTML/CSS/JS frontend
  (no React/npm/build), raw sqlite3 (no ORM), trajectory-shape analysis
  (no exit zones, no turn matrix — though we reinterpreted "no turn
  matrix" as "no hardcoded leg-pair lookup" and used runtime geometry).
- **The user's mental model:** "the trajectories are obvious to the human
  eye — let the data tell us." Reinforced the clustering direction over
  pure geometric rules. Worth weighing in any future classifier work.

## Files most touched recently (in case I need to pick up cold)
- backend/services/trajectory_classifier.py — new score_destination_leg,
  rank-based derive_movement.
- backend/services/pipeline.py — _finalize_vehicle_data, _write_vehicle_event.
- backend/services/v3_aggregator.py — od_matrix output.
- backend/routers/intersections.py — _run_v3_pipeline (mode wiring, frame_progress).
- backend/config.py — PROCESSING_MODES dict.
- backend/database.py — vehicle_events new columns (classifier_*, destination_*).
- frontend/js/setup.js — chip renderer, mode selector, save-labels handler.

## Start here tomorrow
"Picking up the intersection-counter work. Bug #5 is verified fixed. We
were mid-discussion on which mode/sensitivity option to ship for the
'fast mode misses high-speed vehicles' problem. Last user message:
'what about if we just turn up the vehicle detection and tracking to max
sensitivity?' I'd ask the mode question fresh — Balanced / Fast+sensitive /
Both — and proceed from there."
