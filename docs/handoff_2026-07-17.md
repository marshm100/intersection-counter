# Handoff — 2026-07-15 → 07-17 session block

Three workstreams closed, one opened. Everything below is committed on
`claude/accuracy-impl-2026-05-27` and pushed; working tree clean; no
processes left running; suite fully green (798 tests, the old OpenVINO
batch failure fixed for real).

## Shipped

1. **Item 8 CLOSED** (`plan_item8_attribution_walls_2026-07-14.md`).
   Mechanism ① (origin-evidence gate + partial-evidence posterior +
   conservation pass) adjudicated over THREE blind sweeps: proven where
   evidence coverage is high (cam2 9.1→3.3–3.5% per-approach MAE), not
   blind-deployable at low-coverage cameras; retired as default per its
   budget, flags OFF, code+tests revival-ready. All three walls have
   verdicts; SB-right's "1.07 fix" exposed as cancellation.
2. **§3-E deliverables DONE** (`plan_deliverables_E_2026-07-16.md`).
   Miovision-parity workbook (geometry-aware Summary, TMV Table/Data),
   letterhead-configurable PDF (per-minute + 15-min pages), gated
   per-intersection-day export endpoints + card buttons; verified live.
3. **§3-D detector fine-tune DONE AND PROMOTED**
   (`plan_detector_finetune_2026-07-16.md`). Corridor-trained two-class
   head (430 frames labeled in the new in-app labeler, 64 CPU epochs in
   resumable chunks). **Held-out FM51 full-chain gate: total −9.1%→+1.1%,
   interval MAE 8.8%→2.5% PASS, PM low-light miss CLOSED, NB
   −19.2%→+4.6%.** Promoted into the Balanced profile
   (`yolo26s_ft1.pt`@640, class_scheme finetune_v1 → COCO mapping;
   fallback = revert the profile entry). Weights local-only
   (`runs/detect/.../chunk6/weights/` + repo-root copies, gitignored).

## Opened

**The origin-grab cycle** (`plan_origin_grab_cycle_2026-07-17.md`) — the
promoted detector's known residual (FM51 side leg +73 veh / SB −6.5%; same
family as cam4 Wall C). Phase 0 = birth-geometry bucketing on EXISTING
dumps (ftv1_am/pm + cam4 study dumps); mechanism ② (beyond-the-mouth
origin gate) is the lead candidate; replay-only gate. Flag-queue
conservation feeders surveil the class meanwhile.

## Next session (recommended)

Phase 0 of the origin-grab cycle + native articulated wiring (persist the
model's class 1 through cache→events→L/M/A columns, retiring the ~70% size
heuristic; dev scorer = FM51 Mio 102). Same dumps, same harness.

## Session 2 (2026-07-17 afternoon) — phase 0 done, articulated wired

1. **Origin-grab phase 0 DONE** (`phase0_origin_grab_2026-07-17.md`): the
   mid-block signature is **d_main < 25px AND d_mouth > 60px** (born ON the
   crossing road, off the claimed mouth's throat) — 94%/93% of FM51's
   certain/near-certain grabs, 86% of cam4's watch cell, 24% of the
   mostly-genuine group. s_in alone does NOT generalize (cam4's driveway).
   The grabs ride the tier-0 POLYLINE match, not anchor proximity — the
   claim-veto's fallback lands the true origin naturally. Constants frozen
   for phase 1. Evidence: `evaluations/phase0_origin_grab_claims.json`.
2. **Native articulated wiring SHIPPED; gate COMPLETE same day**
   (`plan_articulated_native_2026-07-17.md`, VERDICT section;
   `runs/finetune_v1/fm51_native_artic.json`): ft class 1 → id 8
   end-to-end, vote floor 2 frames, size pass skipped for finetune runs,
   803 tests green. Gate: **no-regression PASS byte-exact** (+1.1% total /
   2.5% MAE / same kill counts); **Articulated bar FAIL — a detector-HEAD
   recall gap** (41/102 semis recognized, 2,287 sustained class-8 dets on
   those 41 tracks, 22 → events). The planned revert was measured STRICTLY
   WORSE (ft chain gives the size pass ≤18 single_unit candidates; the old
   ~95 was the retired coco chain's — a silent class regression at
   promotion, never scored there) → native KEPT as least-bad, revert lever
   still one line. Mediums 7/92 = ft-scheme taxonomy gap (ordinary trucks
   are class 0 by training design). ROUTE: articulated + single-unit-truck
   labels in the fine-tune lane (labeler, corridor only, FM51 held out);
   `fm51_native_artic_gate.py` re-runs unchanged as that cycle's gate.
3. **cam4 study_1100/1600 replays also detached**
   (`logs/replay_cam4_rest.log`, scratch `ic_scratch_cam4/`) — re-check the
   phase-0 signature on them when they land (expect no movement; 0700 +
   FM51 already agree). NOTE the found pathology: a stationary unassigned
   clutter track makes `_assign_origin` rescan its full trajectory EVERY
   frame — quadratic; window 1100 replay runs >40 min vs 87s for 0700.
   Candidate cheap fix (own note, NOT phase-1 scope): cap/throttle origin
   re-attempts for never-moving tracks.

## Session 3 (2026-07-17 evening) — the two smaller items

1. **Replay quadratic FIXED** (`398c913`): incremental tripwire scan in
   `_assign_origin` (per-track `tripwire_scanned` high-water mark) —
   study_1100 replays in 50 s (was 2604 s), all 3379 events BYTE-IDENTICAL
   in the parity re-replay; suite wall-clock 7:14 → 1:50. Invariant any
   future edit must keep: segments below the mark are enter-free for ALL
   legs (that's what makes skipping exact).
2. **Labeling round v2 READY for the operator**
   (`labeling_round_v2_2026-07-17.md`): 164 corridor frames in
   `data/finetune_v2` (semis miner + mixed pass, v1 stems excluded, FM51
   contributes nothing), labeler extended with class 3 "medium" (M key,
   pink) end-to-end — served page build verified pre-handoff (legend/key/
   color present, manifest 164, image 200). Operator: labeler.html?ds=
   finetune_v2, ~1.5–2 h, truck-first order. POST-labeling check owed:
   reviewed.json moved + class-3 rows exist in txts. The retrain plan doc
   owns the v1-medium inconsistency + event mapping decisions.

## Session 4 (2026-07-17 night) — phase 1 run to its phase-2 verdict

Claim-time veto IMPLEMENTED (flag OFF, frozen D_MAIN=25/M=60, all three
claim tiers, 47 pipeline tests incl. the reproduced steal), FM51 ablation
+ full blind corridor sweep + established-scorer scoring DONE same
session (`plan_origin_veto_2026-07-17.md` PHASE-2 VERDICT; evidence in
runs/origin_veto/). **Verdict: NOT ship-ready — the redirect half is
proven (FM51: SB −6.5→+1.0, kills 95→6, S-left 54→5, misplacement
266→127; cam4 watch cell 166→111, approaches flat; cam1/cam3 no-reg),
but where no alternative candidate matches, vetoed tracks DROP: cam2
4.1→6.7 (−467 events, tripwire hit), cam5 approaches worse (−416).**
Next stage is written in the verdict: the RESCUE half — an origin-less
vetoed track claims the through-road its birth sits ON (same d_main<25
identification, direction by motion along the tangent), else
origin-uncertain → queue; plus the finalize-time rewrite leak (FM51
S-right 76→84) respecting the veto set. Re-run the same ablation+sweep
scripts (scratchpad phase0/sweep/score scripts documented in the plan).

Next session: the veto's rescue half + re-gate. Parallel: operator labels
round v2, then the retrain plan doc (finetune_v2 head + gate re-run).

## Session 5 (2026-07-20) — phase 3 built, gated, and RETIRED

The rescue half + rewrite-veto: designed (plan PHASE-3 section, code-level,
against the real finalize chain), implemented under the same flag (no new
constants), 811 tests green incl. flag-off byte-identical, FM51 ablation +
full blind corridor sweep + established scoring re-run on the identical
basis (every OFF replay reproduced its committed total EXACTLY — parity at
scale). **VERDICT: FAIL at the corridor, RETIRED per the two-iteration
budget** (plan PHASE-3 VERDICT; runs/origin_veto/*_phase3.json):

- FM51 improved again (S 133→79, W→1778, leak S-right 76→67 CLOSED,
  rescue returned 810 drops to the main road) — the lone win, both rounds.
- cam2 4.1→7.2 (worse than phase-1's 6.7; −538 events, MORE than −467),
  cam5 EB 16.1→26.1 (worse than 21.0; −412 ≈ −416). cam1/3/4 wash.
- Root cause on the record: at corridor far-field compression the frozen
  d_main<25 signature stops discriminating (cam2: 21,142 vetoed tracks vs
  16,648 events; rewrite-veto gutted 14,002 candidate sets) — the veto set
  loses precision and every downstream consumer amplifies it. Same
  cross-camera class as the item-8 evidence-gate retirement.
- Flag stays OFF (env override kept); code+tests revival-ready; two named
  revival candidates (site-activation precondition via veto-rate; a
  compression-relative d_main needing a fresh phase 0). Conservation
  feeders keep surveilling; the FM51 shipped residual remains known.

Also: the branch's session-2/3/4 commits were never actually pushed
(remote sat at 8b985c2) — pushed now with this session's two commits.

Next session: **labeling round v2 is the lever** (operator: labeler.html?
ds=finetune_v2, 164 frames, ~1.5–2 h) → finetune_v2 retrain → re-run
`fm51_native_artic_gate.py` unchanged (articulated 41/102 + Mediums 7/92).
Small unblocked parallels: backup rotation, subprocess job runner
(plan_C_polish residuals).

## Operational notes

- In-app labeler: `/static/labeler.html?ds=<name>` (datasets under
  `data/`). LESSON PAID FOR TWICE: verify the served page build AND the
  persisted label counts around every operator handoff.
- Detached jobs: Start-Process + Monitor (never background Bash for
  things that must survive turn ends).
- Evidence-coverage (n_origin_evidenced/n_tracks) is the blind health
  check for gate geometry: cam2 80% / cam1 36% / FM51 2–6%.
- FM51 remains the held-out site — never train on it.
