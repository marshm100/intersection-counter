# Handoff — 2026-07-20 → 07-27 session block

Everything committed on `claude/accuracy-impl-2026-05-27` and pushed
(remote verified in sync; the block also cleared a silently-unpushed
10-commit backlog found on 07-20). Working tree clean; no processes left
running (labeler server, monitors, detached jobs all stopped); 816 tests
green.

## Shipped, in order

1. **Origin-veto PHASE 3 — built, gated, RETIRED** (`plan_origin_veto_
   2026-07-17.md` PHASE-3 VERDICT; runs/origin_veto/*_phase3.json). The
   rescue half + rewrite-veto improved FM51 again (S 133→79, leak closed
   76→67) but the corridor failed harder than phase 1 (cam2 −538, cam5
   EB 16→26): the frozen d_main<25 signature doesn't discriminate at
   corridor far-field compression. Flag OFF, revival-ready, two named
   candidates. The gate harness is now COMMITTED (`scripts/
   origin_veto_*`) — the phase-1 drivers died with their scratchpad and
   had to be rebuilt once; never again.
2. **Labeling: round v2 COMPLETE + full v1 medium scan** (operator, both
   handoff-verified): v2 164/164 (120 art / 55 long / 63 med, +646
   boxes); v1 430/430 re-scanned for the medium class (+187 M across 147
   frames — quartile yield [28,50,45,24] vindicated the full scan).
   Combined 594-frame 4-class set (`data/finetune_v12.yaml`, local-only).
3. **finetune_v2 DONE AND PROMOTED** (`plan_finetune_v2_retrain_
   2026-07-20.md` + GATE VERDICT): 64 CPU epochs, winner chunk5 best.pt
   (48 ep) → `yolo26s_ft2` FP16@640. Held-out FM51 vs promoted v1,
   identical basis: total +3.8→+2.0%, MAE 4.0→3.0 (strictly better),
   Articulated 22→56 vs 102, Mediums 7→157 vs 92 (truck mass M+A 213 vs
   194 — the overshoot is largely the ~46 still-missed semis one bucket
   down). Balanced profile = yolo26s_ft2@640 scheme finetune_v2; native
   single-unit id 9 wired end-to-end (ft 2/3 → FHWA 5 → Mediums, aspect
   bypassed, artic precedence, vote floor 2). Fallback = one-line revert
   to ft1.
4. **ft2 corridor re-baseline — the decisive NEGATIVE**
   (`ft2_corridor_rebaseline_2026-07-24.md`): the promoted head's
   detection gains convert to accuracy ONLY where attribution is sound;
   each camera's known defect amplified in signature (cam1 4.0→26.1
   fragmentation, cam2 EB splits 24.8→38.3 with real NB/SB wins
   13.9→8.9 / 12.2→8.6, cam4 mid-block grabs 4.7→11.6, cam5 bank hole
   EB 16→64). **Corridor production stays on the stock-era caches; ft2
   must not be re-detected onto corridor-class cameras until attribution
   hardens.** FM51 promotion stands on its held-out gate; risk class
   defined (complex oblique far-field; conservation feeders = the blind
   alarm).
5. **Evidence-gate ACTIVATION PRECONDITION — SHIPPED, DEFAULT ON**
   (`plan_evidence_activation_2026-07-24.md` + PHASE-1 VERDICT; wiring
   commits 7fe5d2f/6658382/ad3e0b8): the item-8 revival. Per-camera
   GT-free coverage census in every pass-2 (pipeline evidence_mode
   off/probe/on — probe counts, provably alters nothing), C=0.45
   (census-definition gap midpoint; the old "80%" was a denominator
   definition). Census decided all 10 camera×basis cells correctly;
   FM51 stands down ×20 clear; the ft2+gate compound measured DEAD
   (posterior counts the fragment flood — 17.3%). **cam2 production
   re-applied through the real pass-2 (all 3 windows activated,
   0.485–0.564): production interval scorer total 4.8%, NB 3.2 / WB 9.9
   / SB 10.2 / EB 17.2** — under the 5% total bar in production on the
   corridor's hardest camera.

## Where the 5% per-approach bar stands

Total cut: FM51 3.0 (held-out), cam2 4.8 production, cam3 3.2, cam4 4.7,
cam1 4.0, cam5 7.2. Per-approach: the walls, each with a named mechanism —
- **cam2-EB 17.2** — occlusion-split double-attribution (SB-thru-volume
  correlated; the ONE wall left at cam2)
- **cam5-EB ~16** — bank-coverage hole: the live bank has NO EB-thru path
- **cam1-EB ~21, cam4-SB ~9.1** — association residual / mid-block-birth
  origin class (origin-grab: retired veto, surveilled, revival candidates
  on file)
- **Blind-gate validation** (§3-B S1 sensitivity + hardest-condition
  spot-window rule) — required to CLAIM the bar at a GT-free site

## Owed / parked

- `fm51_native_artic_gate.py` hardcoded outp overwrote the v1 evidence
  once (restored from git) — parameterize on next touch.
- Posterior-OFF ft2 ablation (one env flag) — named in the activation
  verdict, parked.
- Future semi-diversity label round (the ~46 missed semis); gate re-runs
  unchanged via its env pointers (GATE_WEIGHTS/GATE_VARIANT/GATE_CLASS_MAP).
- C-stage residuals: backup rotation, subprocess job runner.
- New-variant launch step: recipe botsort+reid needs
  `build_reid_cache.py --camera N --variant <v>` before pass-1 (cost one
  crash-resume this block).

## Operational notes

- **Scoring-basis discipline** (new memory): replay-isolated vs production
  pass-2 vs live-legacy numbers are incomparable — name the basis;
  OFF-parity reproduction proves a basis match.
- Scratch: `ic_scratch_97a7849a` holds re-derivable replay DBs (disk
  filled once this block — deleting extracted-DB classes is safe; dumps/
  caches under data/projects are the expensive artifacts). Weights stay
  local (runs/detect/... + repo-root copies, gitignored).
- Detached jobs: Start-Process + Monitor; labeler handoffs verified
  before AND after (both held this block).
