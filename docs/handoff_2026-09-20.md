# Handoff prompt — intersection counter, 2026-09-20

> STATE AT RESTART. Nothing running. Working tree CLEAN, everything committed and pushed to
> origin/claude/accuracy-impl-2026-05-27 (head 3d4dad1). Production dumps and standings are
> UNTOUCHED at fleet 77.97. Session ended to update Claude Code; STAGE 0 BELOW WAS ABOUT TO
> RUN AND WAS NOT STARTED.

You are the agent on C:\dev\intersection-counter (Python 3.12 venv at .venv; app on
127.0.0.1:5000). Marshall dictates by voice: one question per message, carry the full context
with every ask, plain prose, never a menu. Read his records before stating anything (docs/,
git log); recalled memory is background, not authority. Review clips are full frame, never
cropped.

## What changed this session (2026-09-19/20)

1. **The miss log and the break instrument.** tracker.py `miss_log` (diagnostics only, 11-tuple
   per track that ends a frame without a box: absolute frame, id, was_lost, age, PREDICTED box,
   LAST OBSERVED box, Kalman vx/vy, observed vx/vy, fate); `label_hits` factored out of
   research_tracker_break.timeline (output byte-identical); scripts/research_breaks.py (every
   break joined to the tracker's own state: id status x box fate x first failing gate);
   scripts/viz_break_reel.py. Break reel ruled 4 of 6 real, 2 yardstick chain jumps.
   Page https://claude.ai/artifact/2TzX7VKWYafDJuj9v3ZLki
2. **THE KALMAN VELOCITY IS NOW THE DEFAULT** (`TRACKER_KF_VEL_STD` = 1/20 of box height per
   frame, was the library's 1/160). Declared from the miss log: the filter followed a vehicle's
   growing pixel speed at a fifth to a half. Arms vl1-vl5 re-tracked and scored on the chains;
   every measure improved on every site. one-track 4241/3738/1168 -> 4422/3952/1249, breaks
   2918/4057/766 -> 2547/3637/624, thefts 83/196/18 -> 73/149/13.
3. **Twins census** (scripts/research_twins.py, viz_twin_reel.py): 278/415/54 twin spans;
   nested doubles are born under the IoU-only stacked guard; beside twins are neighbours,
   trailers and occlusion ghosts. TWIN REEL PUBLISHED AND UNRULED — leave it that way for now:
   https://claude.ai/artifact/MwSBH41y4kPEe3bqQGu2NF
4. **ARM vl3-fleet: the tracker priced against the deliverable for the first time in 8 days.**
   Declared before scoring (commit 57fd051), verdict in docs/plan_basis_2026-09-11.md.
5. **The error budget and the problem list** — the triage below.

## The vl3-fleet verdict (the important result)

  fleet of 12: 77.97 -> 79.38 (+1.42). PASSES the G-DEF-1 letter (no camera -1.0: cam4 -0.87,
  margin 0.13; no window -3.0). FAILS the honest test declared in advance — d18b (position
  recovery ALONE, 2026-09-12) reached 80.36, so the six tracker components added since are
  NEGATIVE on the corridor by 0.97.
  FM 51, the held-out blank site: movement 81.6/80.0 (b7) -> 80.4/80.4 (d18b) -> **85.7/89.8**;
  approach 54.2/50.0 -> 62.5/66.7 -> **73.9/79.2**. The best FM 51 numbers on record.
  WHY THEY DISAGREE, measured: cam5 gains because the SB_thru overcount collapses (+158 -> +24
  vs Miovision); cam4 0700 loses because the NB_thru shortfall DEEPENS (-251 -> -360). Pass 2
  refuses a long track whose exit crossing WAS observed but whose destination softmax fails on
  tail motion (G-EX-2, declared 2026-09-12, NEVER BUILT), while the fragments the
  straight-fragment rule used to complete no longer exist. The counting default was tuned on a
  fragmenting tracker; on cam4's northbound the two errors were cancelling.
  RECOMMENDATION RECORDED (his ruling still open): do NOT ship the tracker as the production
  basis yet; build G-EX-2 and re-run THIS arm; stop tracker engineering until then.
  Reproduce: 8 dumps already exist as vl3_* variants (cam4/cam5 study_0700/1100/1600,
  0acb12c0 cam2 vl3_l1_study_0700/1600). Re-score with
  `FLEET_STEM=vl3 FLEET_WORKERS=6 FLEET_ARMS="4:vl3_study_0700,...,5:vl3_study_1600"
  .venv/Scripts/python.exe -X utf8 scripts/fleet_flags.py` then
  `scripts/fm51_arm.py vl3 vl3_l1_study_0700 vl3_l1_study_1600`.

## THE PROBLEM LIST (docs/plan_basis_2026-09-11.md "THE ERROR BUDGET AND THE PROBLEM LIST")

Fleet of 12: 1294 cell-bins, 239 failing (18.5%). Approach bar: 493 bins, 181 failing (36.7%).
Prizes are upper bounds; cam4/cam5 rows measured against the vl3 arm, the rest against production.

  #  problem                        failing bins  direction         prize
  1  cam4 northbound through        24 of 24      under (-721 veh)  +4.16
  2  cam2 RIGHT TURNS (EB/SB/WB)    42 of ~84     over              +3.33
  3  cam5 northbound left           24 of 24      over (+54%)       +1.88
  4  cam5 eastbound right           22 of 24      under (-58%)      +1.72
  5  cam4 southbound through        10            over              +1.72
  6  cam4 southbound right           5            over              +0.81
  7  cam3 northbound through        29 of 42      both ways         +0.68
  8  the approach bar, fleet-wide   181 of 493    mixed             unsized
  Right turns as ONE theme (2 + 4 + 6 + cam5 SB right): ~64 bins, ~+5.9 fleet.

NOT one global bug: the same cell fails in OPPOSITE directions on different cameras (NB thru
under on cam4, over on cam3; EB right under on cam5, over on cam2) -> per-site geometry,
channels and gates, not one algorithm. The tracker is NOT on this list; FM 51 is the healthiest
site in the budget.

## THE PLAN — stage 0 was next and was NOT started

**STAGE 0 (DO THIS FIRST — reading, no compute).** This project has a layer of
already-diagnosed, already-validated, SHELVED counting fixes, and at least two of the top four
problems land on them. Found in 20 minutes before the session ended:
  - backend/config.py L797-853: the cam2 shared-exit collinear confusion was ROOT-CAUSED
    2026-07-02. SB-thru (27->29) and EB-right (28->29) share an exit, run 11 px apart, entries
    171 px apart; mdh calls them the same shape and rewrites origin SB->EB. THAT IS TODAY'S
    PROBLEM 2 EXACTLY (cam2 1600: SB thru 2247 -> 2052, EB right 699 -> 853).
  - `ENTRY_TIEBREAK_ENABLED` = False, DISPROVEN (regressed cam2 33.2->41.0; it is a biased
    sampler of its own worst cases). Do not retry the entry discriminator.
  - `SPEED_TIEBREAK_ENABLED` = False — the retry, VALIDATED NET-POSITIVE on cam2 (EB 33.2->28.8,
    SB 12.3->10.8) and **cam5 (NB 9.0->5.3 — that is PROBLEM 3)**, provably inert on cam1/cam3,
    but REGRESSED cam4 (NB 4.9->5.8). Shelved as per-camera (`calib_speed_tiebreak`) AND inert
    until build_bank supplies `expected_speed` on the paths.
  - `ORIGIN_EVIDENCE_GATE_ENABLED`, `ORIGIN_POSTERIOR_ENABLED`: both off, env-gated, with plans
    (docs/plan_origin_evidence_gate_2026-07-14.md, plan_posterior_half_2026-07-15.md).
  Stage 0 = map ALL EIGHT problems onto three buckets: (a) already diagnosed with a shelved fix,
  (b) diagnosed with no fix, (c) undiagnosed. Read backend/config.py's off-by-default counting
  mechanisms, the docs/plan_*.md counting/attribution verdicts (NOT the tracker ones), and the
  ~270 scripts/ diagnostics (audit_missing_cells.py, diagnose_cam2_eb.py, diag_turn_attribution.py,
  cam5_sbinsuf_diag.py are the relevant starts). THREE PARALLEL Explore agents was the intended
  shape: one on config, one on docs, one on scripts.

**STAGE 1 — the one instrument genuinely missing: the JOURNEY FATE LEDGER.** `vehicle_events`
already explains every journey we COUNTED or REJECTED (origin_leg_id, destination_leg_id, both
posteriors, both margins, posterior_source, rejected). What has NO row is the ~18% of tracks
that produce no event at all (pass2_replay returns a single `insufficient_data` bucket; the
fleet log's "dropped=1371" is that). scripts/cam5_sbinsuf_diag.py already did this for ONE cell
on ONE camera and names the three sites in pipeline.py that increment it. Generalise: one row
per track with disposition, reason, place, time. This is the counting analogue of the miss log
and it makes the UNDERCOUNTS (problems 1 and 4) diagnosable.

**STAGE 2 — diagnose from the side that suits each problem.** Overcounts (2, 3, 5, 6) are
already fully visible in vehicle_events: query the surplus journeys in the failing bins,
cross-tab by origin/destination/posterior_source/margin, film TWO clips for a ruling. Undercounts
(1, 4) need the fate ledger; for problem 1 it splits the far-field DETECTION floor from the
COUNTING refusal, the single most important unknown. Problem 7 needs the per-bin residual against
queue state. Problem 8 is determined by ORIGIN assignment alone, so the ledger's origin fields
attribute it directly.

**STAGE 3 — fix discipline, unchanged:** declare the fix from the measurement, build as a knob,
re-run the 8-window arm, judge on the G-DEF-1 letter, FM 51 held out, one arm at a time.

## THE OPEN RULING (his, not yours) — blocks stage 3 on problems 2 and 3

The shelved speed tiebreak is PER-CAMERA (`calib_speed_tiebreak`). The prime directive says ship
ONE default, never per-window tuning. A per-camera knob the operator sets when DRAWING the site,
alongside the gates and channels, is arguably calibration rather than tuning — but it is not
obviously inside the rule. If per-camera calibration is illegal, problems 2 and 3 need a
different fix and the July work stays shelved. If legal, they may be close to solved already.
Put this to him before building anything on problems 2 or 3.

## Dead ends — do NOT re-run

- A constant TIMESTAMP OFFSET does not explain problem 7. Scored at lags -5..+5 minutes, lag 0
  is already optimal on cam3 0600 (75.4% vs 73.4 either side) and cam2 1600 (32.6 vs 30.6/27.7).
  What remains is VARIABLE delay (our gate line vs Miovision's reference point, growing with
  queue length) or per-bin misclassification that averages out.
- The ENTRY discriminator for the shared-exit cluster (above).
- Everything in the 2026-09-11/12 tracker dead-end list (pixel track-length floor, time floor,
  born-inside rule, single-basis defaults, the theft class beyond the held-box guard).
- THEFTS ARE PARKED: what survives the held-box guard has no geometric handle; the
  discriminator is appearance, which the cache-based tracker does not have.

## On the manual reels (his question, 2026-09-20: "is this productive")

Answer given and accepted: the reels were about half productive. Two of five drove a decision
(hand-off, theft), one gave a useful discount (break), two only confirmed what was already
shipped (back-fill, refusal). NEW RULE: film only when a ruling changes what gets built next,
and TWO clips not six when the purpose is checking the instrument. The yardstick's own error
rate is measured at about a third (3 of 6 theft, 2 of 6 break) and is carried as a discount
instead of re-measured. The genuinely unproductive thing was going 8 days without pricing the
work against the fleet.

## Useful commands

    .venv\Scripts\python.exe -m pytest backend/tests/ -q          # 1280 passing
    scripts/research_breaks.py PROJ CAM VARIANT                   # break -> tracker state
    scripts/research_twins.py PROJ CAM VARIANT                    # two ids on one vehicle
    scripts/research_tracker_break.py timeline_all VARIANT PROJ CAM
    scripts/arm_retrack_alias.py PROJ CAM VARIANT PREFIX          # re-track from cache (~90s)
    scripts/fleet_flags.py / scripts/fm51_arm.py                  # the fleet + blank-site arms
  Arm DBs: data/projects/97a7849a/_replay_scratch/fleet_20260908/ (d17_* = production,
  vl3_* = this arm); FM 51: data/projects/0acb12c0/_replay_scratch/blank_20260911/.
  The full error budget is rebuilt by lifting rule595.compliance's worst_n cap (the saved score
  JSONs keep only 8 failing rows a window and will bias any re-analysis).
