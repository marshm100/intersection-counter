# Handoff — 2026-07-02 (rebuild_flags perf: both feeders fixed)

Branch `claude/accuracy-impl-2026-05-27`, HEAD **`83a4bba`**, all pushed, working
tree clean. Continues `docs/handoff_2026-07-01.md`. Memory:
`project_export_gate_onedrive_perf_2026_06_30` (updated).

## One-line state
The flag-queue **"Rebuild queue" button** no longer hangs: both feeders were the
OneDrive scattered-read antipattern and both are now SQL-aggregate / index-seek.
A single rebuild went from a **multi-minute hang → ~0.5 s warm**.

## What shipped (2 commits)
1. **`5cc9bba` — coverage_qa corridor-gap.** `feed_suspected_gaps` ran
   `coverage_qa._binned_io` for EVERY corridor intersection on every single-
   intersection rebuild, each materialising ~30k event rows in Python (~90k
   rows/rebuild) → **>5 min cold → 0.34 s**.
   - `_binned_io`: SQL `GROUP BY` computing the 900 s wall bin in SQL. Used
     `strftime('%s')` (exact integer seconds) not `julianday()*86400` — the latter
     carried ~1e-5 s float error that flipped boundary-exact events into the
     adjacent bin. Validated **byte-identical** bins + flags on the 90k corridor.
   - `interval_anomalies`: same `GROUP BY` treatment.
   - `interval_corridor_gaps(target_id=…)`: a single rebuild scans only the target
     + its two corridor neighbours (the only pairs that can flag it); kept flags
     identical. Regression test `test_target_id_neighbor_window_matches_full`.
   - Widened `idx_events_cardinal` → **`idx_events_cardinal_v`** (+`video_id`) so
     the binned-IO GROUP BY is index-only. New name keeps the widen idempotent.
2. **`83a4bba` — `feed_uncertain_events`.** It materialised ~30k rows/intersection
   and parsed every posterior in Python to find low-margin near-ties (~18.9 s warm,
   the dominant cost once #1 was fixed) → **0.16 s** warm.
   - New **`destination_margin`** column = precomputed `P(top)-P(2nd)` of the
     posterior, written at event time (`pipeline._write_vehicle_event`) from the
     **serialised JSON** so it's byte-identical to what the feeder recomputes (no
     boundary event slips the pre-filter).
   - `backend/services/posterior.py`: single source of truth (`posterior_margin` /
     `margin_from_json`), used by the pipeline, the backfill, and (implicitly) the
     feeder's `_posterior_top2`.
   - Query seeks only candidates: `detection_confidence < floor OR
     destination_margin < floor OR destination_margin IS NULL` via
     **`idx_events_uncertain`** (EXPLAIN: USING COVERING INDEX). The `IS NULL`
     branch is a correctness net for any un-backfilled row.
   - One-time backfill guarded by an **O(1) `project_info` sentinel**
     (`destination_margin_backfilled`) — the NULL-margin probe is a full scan, so
     it must NOT run every connection. Retrack copies the column through
     (`apply_bank` builds its column list from `PRAGMA table_info`); add-missed is
     `manually_edited=1` (excluded from the feeder). All write paths covered.

## Validation
- **Parity:** `interval_corridor_gaps` new==old flags byte-identical; per-approach
  `_binned_io` byte-identical after the `strftime` fix. `feed_uncertain` flagged
  set == the stored-margin trigger set exactly (iid=1: 1069==1069) — the analytic
  guarantee (stored margin ≡ recomputed margin, both from `margin_from_json`).
- **Tests:** full suite 652 passed at `5cc9bba` (only the 3 known-bad: OpenVINO-GPU
  `test_detect_batch` + `test_video_api`/`test_video_service`). `test_flags` 48 +
  `test_pipeline` 40 green at `83a4bba`.
- **Timing (warm, iid=3 / 30k events):** `feed_suspected_gaps` >5 min → 0.34 s;
  `feed_uncertain_events` 18.89 s → 0.16 s.

## CAVEAT for next session / deploy (important)
Both commits add index (re)builds and #2 adds a backfill. These run **lazily on the
first `get_connection` to EACH project DB**. The corridor (`97a7849a`) is already
migrated. **Every OTHER existing project pays a one-time migration on first
access** — seconds on a normal machine, but **minutes on this 8 GB / OneDrive box**
(the `idx_events_cardinal_v` build alone was 369 s; the margin backfill + index was
~21.6 min on the corridor). Sentinel/`IF NOT EXISTS` guarded → never twice. If a
first-access feels hung, it's this, not a bug.

## Live end-to-end check — NOT captured (environment, not code)
Tried 3× to boot the server and time the real Rebuild endpoint; all blocked:
StatReload restart-loops because it watches `data/projects/*/project.db` while
OneDrive syncs it (use `reload=False`); then OneDrive uploading the 357 MB of
migration writes saturated disk; finally the **8 GB box paged the worker's working
set to 0** (torch/ultralytics/openvino want ~1–2 GB). The app imports fine under
pytest (29 s). To get the number: run `py start_server.py` when the box is idle and
hit Rebuild on the (already-migrated) corridor — expect ~0.5 s warm.

## Next levers (unchanged, from MASTER_PLAN §4)
- **§3-D articulated classifier** (hard; classification not detection; §0 overfit
  trap — no per-vehicle GT; only helps future runs).
- **§3-E.2b** TMV Table pivot (trivial/low-value).
- **§3-D low-light detection** (hardest / most research-y).

## Key files
`backend/services/{coverage_qa,flag_feeders,posterior,pipeline}.py`,
`backend/database.py` (schema/migration/indexes/backfill), `backend/tests/test_flags.py`.
Measurement scripts left in the session scratchpad (parity_binio / parity_uncertain).
