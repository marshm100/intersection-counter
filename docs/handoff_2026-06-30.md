# Handoff — 2026-06-30

Branch `claude/accuracy-impl-2026-05-27`, HEAD `49b6702`. **All four commits below are
pushed.** Read `docs/MASTER_PLAN.md` first (now has §1b, the acceptance metric); this
handoff orients around it. Prior handoff: `docs/handoff_2026-06-29.md`.

## TL;DR
This session shipped the **operator-readiness core** from MASTER_PLAN §3-B/§3-C and the
explicit acceptance metric — the three goals set at the last handoff. The blind-QA flag
queue, the keyboard review worklist, and `AVG |err| ≤ 5%/interval` are all in, suite-green,
app-verified, committed, and pushed. The metric then **measured the next lever**:
per-approach attribution (totals pass, per-approach fails; cam1 vs the hand count = 7.1%).

## What shipped this session
- `d31f03c` **Step B — blind-QA two-feeder flag queue + acceptance gate** (§3-B).
  - **B1** plumbing: `review_flags` table + DB helpers + `routers/flags.py` + frozen feeder contract.
  - **B2** uncertain feeder (`flag_feeders.feed_uncertain_events`): **data-driven** — confidence
    histograms killed half the planned signals. Live: `detection_confidence<0.35`,
    posterior `margin<0.20`. `trajectory_confidence` DEMOTED to corroborating (a <0.5 floor
    flags 40–63%); `destination_confidence`/class DROPPED (dead in practice).
  - **B3** suspected-gap feeder (`services/coverage_qa.py`): S1 corridor-interval (gated on the
    whole-window verdict + **co-active bins** — both fixes from validation that otherwise cried
    wolf), S2 abrupt-anomaly. **Honest documented limit** (validated on FM51): a smooth uniform
    detection sag is NOT internally detectable → that's the spot-count's job, not a fake detector.
  - **B4** gate fold-in (`spot_check.acceptance`): impact units NOT summed — `suspected_gap`
    impact = estimated error (drives verdict vs ±5%), `uncertain_event` = backlog (surfaced).
- `cfed209` **Step C1 — keyboard review worklist** (`frontend/js/worklist.js`). Cursor model
  (open list + `GET /flags/{id}` enriched), one-key actions + auto-advance, both render modes.
  Backend adds: `GET /flags/{id}`, gap-flag clip enrichment. **App-verified** (frame+trajectory
  render; Enter→resolve→advance).
- `4290be0` **Step C2 — batch / stopping banner / live count**. `POST /flags/batch` +
  `batch_resolve_flags` + `B` key ("resolve all N like this"); green "Ready to export" banner on
  gate=ship; running vehicle total. **App-verified** (`B` cleared a 6-flag group in one press).
- `49b6702` **Acceptance metric** `scripts/interval_metric.py` (`summarize_bins`/`per_interval`,
  unit-tested) wired into `triangulate_intervals.py` + `audit_fm51.py`; documented MASTER_PLAN §1b.

## Where we are (validated)
- **Phase B + C complete and app-verified end-to-end:** process → rebuild flags → fly the
  worklist (one keystroke, `B` for groups) → edits write events via /review → live count + gate
  update → "Ready to export." Full backend suite green (~626 passed; only the 2 pre-existing
  unrelated items: OpenVINO GPU `test_detect_batch`, OneDrive teardown rmdir lock).
- **The acceptance bar is now defined and measured.** Baseline vs Miovision per 15-min interval:
  TOTAL squeaks PASS (cam1 4.9% / cam2 2.6% / cam4 3.6% / cam5 2.5%) but **per-APPROACH FAILS on
  nearly every camera** (errors cancel at the total level). The one true-GT check, **cam1 vs the
  hand count = 7.1% FAIL** (Miovision benchmark 2.8% PASS). cam3 (24h) reads 39.7% — a night
  low-volume + coverage-edge artifact (tool flags the no-coverage bins).
- **Ground-truth inventory (verified, see [[project_manual_count_triangulation]]):** MANUAL only
  for cam1 (241 min AM+PM — the validation workhorse) and cam2 (36 min AM). cam3/4/5 = Miovision
  only. cam1 is the only Sunnyvale camera with PM manual.

## What's next (ordered; all now judgeable against the metric)
1. **Per-approach attribution — THE measured lever.** Worst cells: cam2 EB +44% / SB +16%,
   cam5 EB +17% / NB +8%, cam1 NB 7.1% / SB 6.2% / EB 11.8%. Use the flag queue's per-approach
   gap feeder + [[project_turn_residuals_2026_06_08]]. Re-measure with `interval_metric.py`.
2. **B4.2 spot-window diversity** — the honest blind answer to time-localized misses: bias
   `propose_window` toward the hardest UNCOVERED interval + require condition coverage. Validate
   against cam1 PM manual (the one place we can prove it catches a PM problem).
3. **§3-A productize** — wire `flags/rebuild` into the pipeline post-process; add the HARD
   server-side export gate (currently soft — the QA banner reads `acceptance.items`).
4. **Small TODO** — add `interval_metric.py --window` for a peak-only cam3 cut (the 24h artifact).
5. **§3-D / §3-E** — articulated classification + L/M/A buckets + Excel/PDF parity (cam1's full
   Miovision deliverable set = the format example).

## Principles to carry forward
- **Validate before shipping.** It reshaped the design EVERY phase: histograms killed half of
  B2's signals; FM51 proved B3's uniform-sag limit (don't fake a detector); the corridor caught
  S1's cry-wolf (whole-window gate + co-active bins). Look at the data first.
- **Net hides per-interval; per-interval hides per-approach.** The per-approach cut is the truth.
- **Blind deployment** — GT is dev data, the gate is the deployment proxy. Don't per-site-overfit.

## Operational gotchas
- **Server:** launch detached (`Start-Process py start_server.py`). The one running (booted ~15:00
  2026-06-30, http://127.0.0.1:5000) has the committed code (uvicorn reload picked up edits).
- **Playwright MCP** drives the worklist fine (it has no native confirm/alert). Jump straight in
  via `browser_evaluate(() => { AppState.currentProject=..; openWorklist(iid); })`.
- **Console encoding:** the Windows console is cp1252 — keep script `print` output ASCII (a `↔`,
  `−`, `⚠` crashed validation runs). Reasons stored in the DB are UTF-8 and fine.
- **FM51 project `0acb12c0` int2** has freshly-rebuilt flags from the C verification — harmless.
- **git push** stderr-wrap is cosmetic; check the `a..b` line / exit code.

## Tools & data
- `scripts/interval_metric.py` (THE metric; `--camera N`), `triangulate_intervals.py` (vs manual),
  `audit_fm51.py` (vs Miovision), `triangulate_manual.py` (net + per-movement vs manual).
- Flag queue API: `POST /intersections/{i}/flags/rebuild`, `GET …/flags[?status]`,
  `GET /flags/{id}` (enriched), `GET …/flags/next`, `GET …/flags/summary`, `PATCH /flags/{id}`,
  `POST …/flags/batch`. `spot_check.acceptance()` now carries a `review_flags` item.
- Screenshots: `screenshots/worklist_c1.png`, `worklist_c2_batch.png` (gitignored, local evidence).
- Memories: `project_flag_queue_b1`, `project_interval_metric_baseline`,
  `project_manual_count_triangulation`, `project_fm51_audit_master_plan`.
