# Tier-4 — cam5-scoped ft2: the study_0700 damage split + the candidacy path
# (2026-08-13)

The Tier-4 candidate named in Block E4's verdict: cam5 on the ft2 basis
resurrects its right-turn cells from ~zero recall to ~truth (NB_right
5/4/0 → 34/49/36 vs Mio 21/26/35; EB_right near-exact at 0700/1600) and
scored +6.8 / +3.6 at study_1100/1600 — failing G-E4b re-entry only on
study_0700's −7.7. E4 pre-named "a study_0700 damage-split diagnosis"
as this block's entry condition.

## The damage split (run 2026-08-13; v2_phantom_diag, ft2 vs ga3ctrl)

  66.4% (71/107) → 58.7% (64/109). Added slots: 3 (1 failing; phantom
  mass 12 vehicles) — NOT the phantom class. Dominant: SHARED-CELL
  INFLATION, +8 net right→wrong, CONCENTRATED IN THE MORNING BINS:
    EB_thru  6 bins right→wrong — ref 0-2/bin, ft2 counts 8-29/bin
             (07:00-07:45; systematic invention into a near-empty cell)
    EB_left  +3 bins (e.g. 23 vs ref 10), WB_right (21 vs ref 3),
    NB_thru  +2 bins (370 vs 344, 350 vs 312)
  The inflation fades by 08:00+. Signature: a MORNING-CONDITION
  over-attribution stream (early sun angle at 07:00-07:45 is the prime
  suspect), not a whole-window defect and not phantom bookkeeping.

VERDICT on the diagnosis gate: the regression is LOCALIZED (morning
bins, principally EB_thru) and REAL over-counting — study_0700 is not
rescuable by bookkeeping and STAYS stood down. The 1100/1600 windows
are unaffected by this defect class and their +6.8/+3.6 stand.

## The candidacy path (named; production-adjacent — proceeds only with
## the operator in the loop, per the standing apply process)

The apply gate adjudicates PER WINDOW with its own guards — the
2026-08-10 precedent applied 3 windows and stood down 9. cam5
study_1100 (+6.8) and study_1600 (+3.6) on the ft2 basis are proposed
as apply-gate CANDIDATES:
  1. Re-derive candidate artifacts (ft2 replay DBs for cam5 1100/1600).
  2. Validate: `py -X utf8 scripts/v2_apply_gate_validate.py --project
     97a7849a` + `scripts/v2_apply_gate_preflight.py` vs LIVE
     incumbents (read-only; the validator machinery gets read carefully
     before first use with ft2-basis candidates).
  3. IF the gate passes them: operator-approved `adjudicate_apply` per
     window (automatic pre-apply backup, post-verify) — the first
     production accuracy change since 2026-08-10, worth +6.8/+3.6 at
     two cam5 windows on the committed dev yardstick.
  4. Standing caveats: the ft2 basis at cam5 keeps activation OFF in
     both arms (coverage 0.295-0.36 vs control 0.330-0.441 — no flip at
     1100: 0.36 vs 0.441 both under bar; VERIFY per sidecar at
     candidacy time per the coupling law); and the bundle-coherence
     note from 2026-08-10 applies (an applied ft2 artifact is
     operator-run, not a product-path flag — "Confirm & process"
     regeneration is protected by the gate's incumbent logic).
