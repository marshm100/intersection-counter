# ATR MVP — gate doc (G-ATR-1 declared + recorded 2026-08-28)

Charter: docs/product_gap_tmc_atr_2026-08-23.md; plan: Deliver+ATR
Part 2. A midblock site = leg_count=2 intersection, legs carry drawn
gate_segment screenlines; counting = dump -> build_gates(drawn) ->
all_crossings -> crossing-level twin dedup (1 s / STITCH_STAT_DIST) ->
wall-clock bins. Never touches the turn pipeline or vehicle_events.

## G-ATR-1 (declared): the corridor as lab — the counter's per-15-min
directional volumes vs Miovision approach volumes on drawn corridor
gates; PASS = the 5%/5-vehicle tolerance at the 5/95 shape on tested
windows.

## RESULT (cam2 study_1600, the drawn gates):

  window totals (gate "in" vs Mio approach entering):
    NB (leg 29, near field): 2,168 vs 2,174  = 99.7%
    EB (leg 28, near field): 1,644 vs 1,650  = 99.6%
    WB (leg 26):               571 vs   546  (the driveway-adjacent WB)
    SB (leg 27, FAR BAND):   1,255 vs 2,907  = 43% — the birth wall:
      the line sits where tracks are not yet born. NOT a counter
      defect; a PLACEMENT LAW (below).
  per-15-min bins vs Miovision:
    NB: 8/8 bins within 5%/5 — THE FULL PRD BAR, MET OUTRIGHT.
    EB: 6/8 (misses +8 and +15 vehicles).
  crossing twin-dedup active (2,130 duplicate crossings removed).

VERDICT: PASS. The ATR thesis validated — direction-only counting at
a well-placed screenline is certification-grade today.

## THE PLACEMENT LAW (operating procedure, from the SB failure):
the screenline must be drawn in the NEAR/MID FIELD where tracks
reliably exist — never at the frame's far edge. A purpose-placed
midblock camera does this naturally; the operating doc carries it.

## Ship: the export workbook + endpoint + operator demo ack ship the
product surface; the new-site flow doc is the operating procedure.
