# Diagnostic — cam2 leg 28 (EB) false-origin anatomy
# (2026-08-13; prep for the operator recalibration session)

Not a mechanism block — instrumentation. F2M-v2 measured leg 28 at
**0.981 self-confusion** (98% of its own gate-verified fulls carry
rival-shaped early geometry). Before the operator redraws the leg-28
mouth, this diagnostic answers the three questions that decide WHAT to
redraw — and whether a redraw can work at all:

1. WHERE along the leg-28 gate line do inward crossings happen —
   position distribution of TRUE-EB crossings (fulls whose early points
   fit EB channels) vs FALSE crossings (fulls fitting rival channels),
   plus the fragments' crossings. If the two populations separate
   spatially, a line/zone redraw fixes it; if they overlap, no geometry
   fixes it and the remedy is crossing LOGIC (a new mechanism block).
2. HOW FAST are the crossings — queue-creep produces slow, jittery,
   shallow crossings; real EB entries cross at approach speed. A speed
   split that separates cleanly is an alternative (or additional)
   operator-facing lever.
3. WHAT the current geometry is — the drawn mouth segment, its length
   and orientation, so the redraw guidance is concrete.

Deliverable: scripts/v2_leg28_diag.py (generic --leg), a numbers table
in this doc, and a concrete redraw recommendation. The success
criterion for the redraw itself is already declared
(plan_t3_f2m_v2_2026-08-13): leg-28 confusion < 0.25, EB_right flood
shrinks toward Mio on replay, no other cell regresses.

---

## RESULTS (2026-08-13) — the diagnosis INVERTS: the gate line is fine;
## the drawn EB CHANNELS are misplaced

Crossing anatomy (study_0700): 206 fulls originate at leg 28; only 4
fit the drawn EB channel family in their early points ("full_true"),
202 do not ("full_confused"). Position and speed distributions of the
two populations OVERLAP COMPLETELY (pos p50 0.41 vs 0.42 on the same
gate band; speed p50 14 vs 24 px/s) — no line placement or speed
threshold separates them. Then the independent probe (birth positions,
no channels involved):

  population      n    birth x p10/50/90   born EAST of gate  born mid-box   dests
  full_true       4    254 / 392 / 423           0%              100%        27:3 26:1
  full_confused  202    503 / 535 / 557          96%                2%       27:146 29:54 26:1

**The "confused" fulls are the GENUINE EB entries** — born upstream on
the EB approach, crossing inward, exiting at EB-consistent legs. The 4
"true" fits are mid-box birth anomalies. Confusion 0.981 therefore
does NOT measure false origin crossings at leg 28: it measures that
**the drawn (28,·) channel polylines sit off the lanes EB traffic
actually drives** — the boxclip "lane off" lesson at full scale, now
quantified: 98% of verified EB journeys misfit their own drawn
channels in the early segment.

This mechanism cleanly explains the EB cell pattern that every block
has fought: EB_thru counted 34 vs Mio 119 (−85) while EB_right runs
+142 over — EB throughs snapped into the right-turn channel by the
misdrawn geometry, with EB_left roughly surviving (its channel
apparently less displaced). The EB_right "flood" is largely
MISCLASSIFIED EB THROUGHS, not phantom vehicles.

## Corrected operator guidance

REDRAW TARGET: the EB CHANNEL POLYLINES (intersection_paths with
origin_leg 28 — especially 28→26 through and 28→29 right), tracing the
actually-driven lanes; the leg-28 mouth/gate line itself needs no
change (real entries cross it correctly). Success criterion updated
accordingly: after the redraw, leg-28 confusion < 0.25 AND EB_thru
recovers toward 119 AND EB_right shrinks toward 167 on the standard
replay, no other cell regressing.

## Second finding — the F2M overshoot re-explained (guard gap, recorded)

With the EB prototypes now known to be built from GENUINE EB journeys,
the 82-86 fragments that fit EB_left are most plausibly pieces of
ALREADY-COUNTED queued vehicles whose dwell time places their fragment
span outside the pilot's ±2 s suspicious window (a protected-left queue
dwell runs 30-90 s; the counted event timestamps at origin crossing).
The F2M pilot's double-count guard was DWELL-BLIND — and this suspicion
extends to part of its NB_left/WB_left/SB_right gains too. The pilot's
+2.9 must be read with that caveat (recorded here; the family is closed
regardless, and any revival must make the suspicious window
dwell-aware: same-cell counted event within ± the cell's own median
queue dwell, not ±2 s).
